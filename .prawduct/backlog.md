# Backlog — Hallucinote

<!-- Work discovered during sessions but out of current scope.
     Add items at the top. Each is a bullet with source marker:
     (builder), (critic), (reflection), or (migrated).
     Review with /janitor or when planning new work. -->

- **Arc 4 / D4 deferred — per-song REQUIREMENTS.md still lists internal Live class names.** 4 songs' `REQUIREMENTS.md` files (regenerated by `compat write-requirements <slug>`) reference devices by their internal class (e.g. `DrumGroupDevice`, `Compressor2`) rather than browser display names. These are derived docs — source of truth is the DB + `captured_session.json` (both migrated). Regen will pick up the post-D4 convention automatically. Sized: trivial (4 `compat write-requirements` invocations + commits). (Arc 4 / D4 cumulative Critic 2026-05-22)

- **Arc 4 / D4 deferred — `resources/reference/device-params.json` keyed by internal class name without post-D4 note.** Informational parameter reference for agents; keys are Live's internal class names (`Compressor2`, `Eq8`, etc.) without a `_post_d4_note` field clarifying that `display` is the new load selector. Not load-bearing — this resource is consulted for parameter discovery, not for `kind` resolution. Sized: trivial (one note field + a clarifying README block). (Arc 4 / D4 cumulative Critic 2026-05-22)

- **Arc 4 / D4 deferred — backlog cleanup for D4-superseded items.** Several pre-D4 backlog items reference the deleted translation table or the round-trip problems D4 resolved. Audit + mark-resolved with archaeology preservation per backlog convention. Sized: bookkeeping. (Arc 4 / D4 cumulative Critic 2026-05-22)

- **Arc 3 deferred — live verification end-to-end for `compat check --probe` + `pull_cli execute`.** Unit tests cover both wire shapes against the production schema via per-helper fake `send_fn`s, but full-loop verification against a real Live session was deferred (Control Surface slot wasn't enabled in this session). Sanity-pass against a real DB+Live on first compose-time use is still worth surfacing. Sized: trivial (one CLI call each, both already documented in build-plan/skills). (Arc 3 cumulative Critic 2026-05-21)

- **Arc 3 / C2 follow-up — `parse_path_shape` rejects empty interior segments.** Today `parse_path_shape('Drums//Kit')` produces `path_prefix=['']`; Live's browser can't match an empty segment, so it surfaces as `kind_unresolvable` at probe time. Acceptable but indirect. A one-line addition (reject empty interior segments alongside the existing empty-pattern guard) would fail loud at the authoring boundary instead. Sized: ~5 LoC + 1 test. (Arc 3 cumulative Critic 2026-05-21)

- **Arc 3 / C3 follow-up — `pull_cli execute --dry-run`.** Today `execute` always applies; the user sees diffs in `ApplyResult.details` AFTER write. A `--dry-run` flag would show the diff before committing. Requires either SAVEPOINT/rollback wrapping around `apply_pull_results` (the apply path uses `transaction(conn)` internally, so a SAVEPOINT-around-the-context pattern would work) or an in-memory DB clone. Pick when the workflow shows it's needed. Sized: small if SAVEPOINT path; medium if clone path. (Arc 3 / C3 build-plan deferral 2026-05-21)

- **Arc 3 / C3 follow-up — `/snapshot-bake-recent-changes` skill markdown.** C3 shipped the `pull_cli execute` CLI; a thin skill wrapper would surface it as a user-facing affordance ("bake my mid-session tweaks before re-pushing"). The skill walks: probe → show diff to user → confirm → run `pull_cli execute device-parameters`. Sized: trivial (markdown only, no code). (Arc 3 / C3 build-plan deferral 2026-05-21)

- **Arc 2 deferred — MCP dispatcher auto-`kind='mutate'` parent.** When an MCP write call lands with no `parent_id` in the request envelope, the dispatcher could open a `kind='mutate'` request automatically so every write gets degraded-but-present provenance. Blocked today by the dispatcher having no DB awareness — which DB? per-call lookup by song_slug from validated params? A future chunk should: (a) add a notion of "ambient DB context" to the dispatcher (per-process default + per-call override), (b) auto-open the parent before handler dispatch, (c) close on success/failure. Sized: medium (touches dispatcher, schema, every write-action contract). (Arc 2 cumulative Critic 2026-05-21)

- **Arc 2 deferred — `hallucinote://annotations/<song_slug>` templated MCP resource.** B2 shipped the `ableton_annotation` tool but not the parallel resource for cheap full-song annotation reads. FastMCP supports templated resources via `{slug}` placeholders, but adding one requires separate test-surface plumbing (the existing `RESOURCE_URIS` lock + `registered_resource_uris` introspection treat resources as a fixed list; templates need a sibling registry walk). Workaround today: `ableton_annotation(action='list', song_slug=...)` returns the same data. Sized: small (resource registration + RESOURCE_TEMPLATE_URIS tuple + test plumbing). (Arc 2 cumulative Critic 2026-05-21)

- ~~**Arc 2 deferred — `ableton_annotation` live verification end-to-end.**~~ **RESOLVED (fix/annotation-handler-sqlite-import-breaks-live 2026-05-21)** — first live load against Ableton 12.4 surfaced exactly the gap this item flagged: `handlers/ableton_annotation.py` had an unguarded `import sqlite3` at module top, which crashed the Remote Script load (Live's embedded Python lacks `_sqlite3`) and cascaded up through `actions/__init__.py` to abort the entire Hallucinote Control Surface. Fix removes the dead import (lazy type-string annotations never needed it at runtime) and adds an AST-based regression test (`hallucinote_mcp/tests/unit/test_remote_script_import_safety.py`) that walks every action/handler/transitive top-level module and refuses imports of stdlib modules known absent from Live's embedded Python. Original framing kept below for archaeology. Unit + handler tests cover the action surface; full-loop verification against a real Live session was deferred because materializing the new tool on the Remote Script side requires `/ableton-mcp-install` + Live restart. The tool is `runs_server_side=True` so it dispatches entirely on the FastMCP side without touching Live, but a sanity-pass against a real DB on first compose-time use is still worth surfacing. Sized: trivial (one MCP call + DB read). (Arc 2 cumulative Critic 2026-05-21)

- **`Kit.assert_has(*names)` strict mode — refuse when the kit has no captured mappings yet.** Empty-mappings kits (pre-capture state — no `pad_info` has been dispatched for this device yet) currently pass `assert_has` for every canonical via the GM fall-through path in `try_pitch_of`. A `build.py` that calls `assert_has('ride', 'crash')` before the first push silently passes, then on the next session `kit.pitch_of('ride')` could raise on the wrong-sound case once `drum_pad_mappings` is populated. Documented design choice (Arc 1 / A3 Critic note 4) but worth surfacing as a future `Kit.assert_has(*names, strict=True)` mode that refuses on empty mappings — "I want to know the kit's pads are characterized AND deliver these canonicals," not "the kit can deliver these canonicals or fall through to GM." Sized: ~10 LoC + 2 tests + docs note. (Arc 1 / A3 cumulative Critic 2026-05-21)

- ~~**Hot Rod Kit confirmed as cautionary tale for GM-derived drum mapping (re-surfacing prior backlog item with a concrete repro).**~~ **RESOLVED (Arc 1 / A3, 2026-05-21)** — `Kit.pitch_of` now raises (with a teaching message naming the colliding chain) when the kit has populated mappings but no canonical match AND the GM-default note is taken by a differently-named chain. `Kit.try_pitch_of` returns `None` for the same case. `Kit.assert_has(*canonical_names)` provides fail-fast bulk validation. `push_cli execute` auto-populates `drum_pad_mappings` after a successful devices phase via the M1-C `pad_info` action (best-effort, idempotent via `replace_drum_pad_mappings`). Next `build.py` reads kit-specific notes via `Kit.from_device`. `docs/song-authoring-conventions.md` "Drum kits: probe, don't assume" subsection documents the three resolution paths. Closes the cowbell-on-metal disaster structurally. Original framing kept below for archaeology. sun-zone-done's drum parts assumed standard GM pads (51=ride, 52=china, 53=ride-bell, 46=open-hat) and the metal sections played heavy 8ths on pad 51 expecting a ride. Hot Rod Kit's pad 51 is "**Cowbell Fenk Chick**" and the kit has NO ride / NO china / NO open-hat at all — slot 46 is another *closed* hat ("Buttery"), 52+ are unmapped. Result: a song whose metal sections clang on cowbell every 8th note. The user (correctly) flagged this audibly within minutes. **This is the exact failure mode the existing "Drum Rack pad-mapping discovery — don't assume GM-derived mapping" backlog item predicted.** Sun-zone-done makes the case for prioritization: this is the canonical 16-pad rock kit shipped with Live, and even *it* doesn't follow GM. The fix needs to escalate to top-of-backlog. Concrete repro and remediation pattern is now in `songs/sun-zone-done/build.py` (Hot Rod-specific pad constants + `pad_info` probe used to discover them). (sun-zone-done 2026-05-20)

- **`build.py` re-run produces a different `song_id` even without `--reset` — breaks ableton_sessions binding.** Ran `python build.py` (no flags) after adding an envelope-authoring call; expected idempotent re-converge per W12-A; got a fresh song_id (`d988a7d5...` vs the previous `570868256...`). Effect: the existing `ableton_sessions` row pointed at the OLD song_id, so `probe-and-link <session_id>` couldn't find the song; I had to mint a new auto-session. Then the new session's push had a stale view of which devices Live already had loaded, and added 4 duplicate devices (extra Glue Compressor on drums, extra Chorus-Ensemble on organ, two duplicate reverbs) plus 2 failed Tension reloads. Smells like the build_session context creates a fresh `songs` row on every run rather than reusing the existing row keyed by `name='sun-zone-done'`. If so, the "state-converger" promise of W12-A is partially broken — the score-half converges but the song row itself doesn't. Fix: `build_session` should look up the song row by `name`; create only if missing; reuse song_id on subsequent runs. Sized: small-medium. (sun-zone-done 2026-05-20)

- ~~**Push planner duplicates devices when re-pushing into a Live set that already has matching devices (no idempotency on the devices phase).**~~ **RESOLVED ARCHITECTURALLY (W20-A, pre-Arc-1)** — `probe_and_link` accepts `live_devices_by_parent` and `_match_devices_for_linked_parents` binds devices by `(parent_kind, parent_index, position, class_name)` so subsequent `_emit_device_calls` skips correctly. Confirmed by code audit during Arc 1 scoping (2026-05-21). Original framing kept below for archaeology. After fresh probe-and-link (new session_id), the push planner walked the DB's 27 devices and tried to load all of them onto Live — even though tracks 5-11 already had the full chains from a prior push. Result: 4 duplicate device loads succeeded, 2 failed (Tension refused to load on a track that already had a Tension instrument). Other phases (tracks, returns) showed "skipped (idempotent)" — so SOMETHING in the planner knows about prior state, but devices phase apparently doesn't. Either (a) devices phase should structurally dedupe (match by chain position + class name + display name against Live's current device list), or (b) the agent-side workflow needs a documented "I'm re-pushing into a populated Live set, please dedupe" mode. Concretely, this is what made the partial-push recovery so painful in this session. Sized: medium. Closely related to the "Push-state coherence" architectural item lower in the backlog. (sun-zone-done 2026-05-20)

- **Manual MCP device-param tweaks during mix iteration don't round-trip back into the snapshot — they're lost on next full re-push.** During sun-zone-done iteration I set Amp Type, Input Gain, sidechain source+threshold+release, Wavetable Sub/Glide/Decay via direct MCP `set_parameter` calls. These produce the right Live state but live nowhere on disk. Next push that touches these devices either loses them (if the device gets reloaded with a different snapshot's `params_dialed`) or preserves them by luck (if the planner doesn't re-touch the device). For a true compose-iterate loop, mix-time tweaks need an automatic path back to the snapshot. The `/song-snapshot` skill does this manually (diff-and-confirm) but it's heavy. A lighter `/snapshot-bake-recent-changes` that captures *just* the parameters changed since the last push, with a clear diff, would close this loop. (sun-zone-done 2026-05-20)

- ~~**No `cue_delete_all` or "force-recreate" for cue points — re-push fails on the cue phase when a cue already exists at the same position.**~~ **RESOLVED (R-1.1 2026-05-20)** — `ableton_arrangement(cue_create / cue_create_batch)` gained `if_exists={"refuse", "skip"}`. Batch default is `"skip"` (idempotent — same-name same-position no-ops with `skipped=true`; name mismatch still refuses so rename intent goes through `cue_rename` explicitly). Planner `plan_push_cue_points` emits `if_exists="skip"` so the next re-push of sun-zone-done is a no-op on the cues phase. Original framing kept below for archaeology. Re-pushing sun-zone-done after adding a channel-switch envelope failed at the cues phase with `cue_create: a cue already exists at position_beats=0.0 (name='intro'); use cue_delete first if you want to replace it`. The cues phase has no idempotency or upsert path — it tries to *create* each cue and refuses if any conflict. Either (a) the planner should detect existing cues by position+name and skip them (true idempotency), or (b) the planner should delete-then-create as the default behavior on re-push. The current "halt and tell the user to delete" is unfriendly during iteration. Sized: small. (sun-zone-done 2026-05-20)

- ~~**Ambiguous preset_query patterns: strict-mode loader is correct but the author has no pre-flight check.**~~ **PARTIALLY RESOLVED (R-2.1 2026-05-20)** — `compat check` now reports `kind_ambiguous` (2+ matches) and `kind_unresolvable` (0 matches) when called with a `browser_dry_runs` map. The pure module is wired; the CLI orchestration that runs `ableton_browser(action='search')` to populate the map is still backlog (the agent or skill can do it manually for now via the new module-level `_dry_run_key`). Original framing kept below for archaeology. **Ambiguous preset_query patterns: strict-mode loader is correct but the author has no pre-flight check.** After fixing roots + path_prefix shapes on sun-zone-done, the second push still failed on 4 devices because my `pattern` strings matched multiple presets: `"Boom in E"` matched `Boom in E.adv` AND `Full Boom in E.adv` (substring); `"Hall"` matched 12 Hybrid Reverb hall presets; `"Room"` matched across folders; `"Vintage Delay"` matched the FOLDER, not a leaf preset (there's no `Vintage Delay.adv` — that's the parent folder name in the browser tree). Strict refusal is right (no fuzzy picks), but the author hits the wall at push time rather than compose time. Fix: extend the compat-check device walker to dry-run every `preset_query` via `ableton_browser(action='search')` and report which ones match 0 / 1 / 2+ presets. 0 and 2+ get flagged with their match counts and the suggested disambiguation (regex anchor, tighter path_prefix, or explicit `.adv` suffix). Sized: small (one new bucket in compat, threading the dry-run through). Folds into the broader "compat check should validate preset_query.root + kind/class" item above. (sun-zone-done push 2026-05-20)

- ~~**`docs/snapshot-schema.md` doesn't distinguish "load the device default" from "load a specific preset" in the preset_query story.**~~ **RESOLVED (R-2.2 2026-05-20)** — `docs/snapshot-schema.md` gained a "Default-device vs named-preset" subsection under "Picking built-in content presets" with worked examples of both shapes (no `preset_query` + `kind: "Hybrid Reverb"` for default; `preset_query` pointing at a `.adv` leaf with `path_prefix` for named). Same pattern called out for Echo/Vintage Delay. Original framing kept below for archaeology. **`docs/snapshot-schema.md` doesn't distinguish "load the device default" from "load a specific preset" in the preset_query story.** The browser tree shows `Hybrid Reverb` as a loadable node (loads the device with default preset) AND `Hybrid Reverb/Hall/` (a folder of preset leaves). I authored `preset_query: {pattern: "Hall", path_prefix: ["Hybrid Reverb"]}` thinking it would load *a* Hall preset; loader correctly refused as ambiguous. The right shapes are either (a) no preset_query at all + `kind: "Hybrid Reverb"` for device default, OR (b) preset_query targeting a specific `.adv` leaf. Doc could spell this out with a worked example pair: "default device" vs "named preset". Same applies to Echo (`Vintage Delay` is a folder, not a preset). Sized: small (doc edit). (sun-zone-done push 2026-05-20)

- ~~**No single-command default-scaffold cleanup after first push (W18-D workflow is half-implemented).**~~ **RESOLVED (R-1.2 2026-05-20)** — `push_cli cleanup-default-scaffold <session_id>` ships. Pure planner `push.plan_cleanup_default_scaffold` refuses on non-canonical unmatched parents (the user must hand-resolve "another song's tracks") and on "would empty Live tracks" (Live's ≥1-track constraint). CLI dispatches in-process `ableton_track/return(delete)` in descending index order, then re-runs `probe_and_link` to reconcile shifted indexes. `.claude/skills/ableton-push/SKILL.md` Step 2a now points at the subcommand. Original framing kept below for archaeology. **No single-command default-scaffold cleanup after first push (W18-D workflow is half-implemented).** First push onto Live's canonical 4-default-track + 2-default-return scaffold leaves the defaults at indices 1-4 (tracks) and 1-2 (returns). The probe-and-link output flags them as `default_scaffold_unmatched_tracks`. The W18-D skill doc says this cleanup is "explicit, user-opted-in" — but no actual command implements the cleanup. I had to delete 4 tracks + 2 returns via 6 individual `ableton_track/return(action='delete')` MCP calls in descending index order, then re-run `probe-and-link --probe` to reconcile shifted indices. A `push_cli cleanup-default-scaffold <session_id>` subcommand that does all of this atomically (descending deletes + reconcile in one call) would close the gap. Sized: small. (sun-zone-done push 2026-05-20)

- ~~**Snapshot device `kind` field is undocumented and unused.**~~ **RESOLVED (R-2.2 2026-05-20)** — `docs/snapshot-schema.md` now documents `kind` as **informational only**: a free-form classification hint that the loader does NOT read. The push loader routes by `class` and `preset_query.root` only. Safe to omit; if included, treat as author commentary. Original framing kept below for archaeology. **Snapshot device `kind` field is undocumented and unused.** snapshot-schema.md shows `"kind": "instrument"` in the example but never says what valid values are or what the field does. I wrote `"kind": "instrument"`, `"kind": "audio_effect"`, `"kind": "instrument_rack"` based on guessing; push planner didn't reject any of them but also didn't use them — `class` is what's threaded to the loader. Either drop the field from the schema OR document it as authorship metadata (with valid values). The risk: a future reader (human or LLM) might assume `kind` matters for routing and waste cycles getting it "right." Sized: small (one paragraph + maybe schema validator). (sun-zone-done push 2026-05-20)

- **Per-section parameter automation (esp. enum params like Amp.Type) is a missing authoring tool.** sun-zone-done's "channel-switched guitar amp" design (Clean amp for reggae sections, Heavy for metal sections) needed Amp.Type automation per clip / per section. The hallucinote envelope API supports continuous-param automation cleanly, but enum-param automation (e.g., Amp Type = "Clean" → "Heavy" at section boundaries) wasn't obvious from the API surface. Two viable shapes to investigate: (a) clip-level enum-param automation via the envelope mechanism with discrete breakpoints; (b) two Amp devices in the chain + per-device on/off automation per section. Either way, "alternating sections want different patches/devices on the same track" is a recurring authoring pattern (this song, any genre-mashup song, any song with verse/chorus tone differences) that deserves a documented recipe. Sized: medium (investigate + document; possibly add helper API). (sun-zone-done push 2026-05-20)

- ~~**`docs/snapshot-schema.md` preset_query gaps caused 11/13 push failures on sun-zone-done.**~~ **RESOLVED (R-2.2 2026-05-20)** — `docs/snapshot-schema.md` consolidated edit: `preset_query.root` enum enumerated inline with the typo callout (`effects` vs `audio_effects`); `path_prefix` documented as JSON list with wrong/right examples. Compat check now structurally validates both at compose time (R-2.1) — the doc and the validation agree. Original framing kept below for archaeology. **`docs/snapshot-schema.md` preset_query gaps caused 11/13 push failures on sun-zone-done.** Two hand-authoring traps that the docs don't surface:
  - **`path_prefix` must be a JSON list, not a string.** Docs describe it as "list of name segments" in `ableton_browser(action='search')` help but the snapshot-schema preset_query example doesn't show a path_prefix at all, and "Hand-authoring tips" doesn't flag the shape. I wrote `"path_prefix": "Tension"` (string) and the loader raised `preset_query.path_prefix must be a list`. 3 failures.
  - **Valid `root` values aren't enumerated in snapshot-schema.md.** The MCP browser RESOURCE uses `ableton://browser/effects` (plural). The preset_query LOADER accepts `audio_effects` (per `ableton_browser` action='search' enum: instruments / audio_effects / midi_effects / drums / plugins / samples / user_library / packs). I used "effects" → 8 failures. Schema doc should either enumerate the valid set inline or link to the load handler's enum.
  - Fix: one-paragraph doc edit in snapshot-schema.md "Picking built-in content presets" section with both shapes spelled out + a fully-loaded multi-device chain example using path_prefix correctly. Sized: small. (sun-zone-done push 2026-05-20)

- ~~**`compat check` should validate preset_query.root + kind/class at compose-time, not at push-time.**~~ **PARTIALLY RESOLVED (R-2.1 2026-05-20)** — `compat.classify_preset_query` catches the structural traps (root not in enum, non-list `path_prefix`, missing root, garbage JSON) and emits `preset_query_invalid`. With a `browser_dry_runs` map, the module also surfaces `kind_unresolvable` (0 matches) / `kind_ambiguous` (2+ matches) / `preset_query_unverified` (no dry-runs). `has_issues` flips True for every new status. The CLI orchestration that runs `ableton_browser(action='search')` to populate the dry-runs map is still backlog. Original framing kept below for archaeology. **`compat check` should validate preset_query.root + kind/class at compose-time, not at push-time.** sun-zone-done's `compat check` returned clean (27/27 native, exit 0) — but 13/27 devices then failed at push for `root='effects'` (typo for `audio_effects`) and bad `kind` strings. Compat is the natural gate to catch these before the user attempts a push. Concretely walk every device in the DB and (a) refuse if preset_query.root is not in the valid root enum; (b) dry-run the kind/preset_query against `ableton_browser(action='search')` to confirm it resolves to a loadable node. The compat infrastructure already partitions by status — add a `preset_query_invalid` or `kind_unresolvable` bucket. Would have saved the partial-push debug loop entirely. Sized: medium. (sun-zone-done push 2026-05-20)

- ~~**`class` field in snapshot vs loader's `kind` parameter — mapping is ambiguous for devices whose Live class != display name.**~~ **RESOLVED (R-2.2 2026-05-20)** — `docs/snapshot-schema.md` now documents that the loader accepts either the class name OR the display name as its `kind` parameter, and that for stock devices whose class differs from the display (`Glue` → "Glue Compressor", `ChorusEnsemble` → "Chorus-Ensemble"), the **display name** is what works at load time. Names the canonical `device_names.class_name_to_display` table as the lookup. No schema field added — the doc clarification was sufficient. Original framing kept below for archaeology. **`class` field in snapshot vs loader's `kind` parameter — mapping is ambiguous for devices whose Live class != display name.** snapshot-schema.md says `class` is "Live's device class name" with examples Compressor2 / DrumGroupDevice / Eq8. The push planner passes this `class` value through as `kind` to `ableton_device(action='load')`, which accepts either class name OR display name. For Glue Compressor I guessed `class: "Glue"` (stripped-down internal name, by analogy with "DrumBuss"); loader needed `"Glue Compressor"` (display name with space). Same for Chorus-Ensemble — I wrote `"ChorusEnsemble"`, loader needed `"Chorus-Ensemble"` (with hyphen). 2 failures. Two fixes to consider: (a) extend the snapshot schema with a separate `display_name` field that's preferred for loading; (b) document the canonical class-name → display-name table somewhere discoverable (or link to `device_names.class_name_to_display`). Either way, the snapshot author needs a reliable way to write the loader-compatible string. Sized: small (docs) or small-medium (schema field). (sun-zone-done push 2026-05-20)

- ~~**Partial push has no documented resume path.**~~ **RESOLVED (Arc 1 / A5, 2026-05-21)** — closes all three concrete asks from the original entry: (1) `.claude/skills/ableton-push/SKILL.md` gains a "Recovering from partial push" subsection naming the diagnose → fix → rebuild → re-run loop; (2) explicitly **no** `--resume` flag — W20-A's device-binding idempotency makes re-run the structural recovery, a separate resume mode would duplicate what's already there; (3) `push_cli execute` FAIL summary appends the verbatim recovery command (`push_cli execute <session_id> --song <slug> --probe`) so the agent doesn't have to reassemble flags. Original framing kept below for archaeology. push_cli execute halted mid-devices-phase with 14/27 devices loaded into Live. To re-push cleanly I had to think hard about whether re-running execute would (a) skip the 14 already-loaded devices and only retry the 13 failed, (b) duplicate the 14, or (c) require manual Live cleanup first. The `.last-push-state.json` file records what was attempted but the skill doc doesn't say "re-run after fixing the snapshot and it'll converge" vs "you must reset Live state first." Concrete asks: (1) skill doc gets a "Recovering from partial push" section; (2) maybe a `--resume` flag that explicitly skips devices recorded as ok in the state file; (3) at minimum, the FAIL summary in the execute output suggests the next command verbatim. Sized: small (docs) to small-medium (resume flag). (sun-zone-done push 2026-05-20)

- **Cross-song shared drum-kit mappings (post-M1-C).** M1-C ships `drum_pad_mappings` scoped per-song (rows reference `devices.id`, which is per-song). But the same Drum Rack `.adg` loaded on machine A and machine B will have the same pad layout (chain names + MIDI notes are kit-intrinsic). Hoisting mappings into a shared layer keyed on `(preset_uri OR preset_query OR plugin_identity)` would let one capture run benefit every song using that kit — and benefit other users if we ever ship a community-share path. Aligns with project memory `project_cross_song_reuse` (shared kits/grooves/templates table family). Deferred from M1-C scoping 2026-05-20: per-song works fine for v1.0; cross-song hoist is a non-trivial schema and ownership design (who owns the mapping when two captures disagree). Sized: medium. (M1-C scoping 2026-05-20)

- **`apply_feel` can produce negative `start_beats`.** A `feel={0.0: -0.02}` dict on bar 1 with `start_beat=0` yields a note at `start_beats=-0.02`. Behavior is pinned by `test_kick_stumble_feel_shifts_downbeat_early`, but the wire path through `ableton_clip(action='replace_notes')` is not tested with negative beats. Three resolutions to evaluate: (a) clamp to `max(0, start_beat + offset)` inside `apply_feel`; (b) clamp at the caller / generator boundary; (c) leave as-is and add a wire-side test confirming Live accepts/rejects gracefully. Sized: small. (W17 cumulative Critic note, 2026-05-20)

- **`docs/song-authoring-conventions.md` "Per-part feel" rule 2 says `feel` accepts a string OR a dict — the generator API is dict-only.** `Feel = Mapping[float, float] | None` would crash on a string literal. Doc gives the impression that `feel="lazy back-half"` is a valid call shape. Reword to make clear the string form is LLM-side intent (the model interprets "lazy back-half" and emits a dict); the value reaching the generator is always a dict. One-paragraph doc edit. (W17 cumulative Critic note, 2026-05-20)

- **`.claude/skills/song-pick-instruments/SKILL.md` step 4 has a backwards arrow example.** `Wavetable → InstrumentVector` is the reverse of `device_names.class_name_to_display`'s direction. Handler behavior is correct (Pass 1 + Pass 2 accept both), but the example misleads readers about the canonical naming direction. One-line nit. (W17 cumulative Critic note, 2026-05-20)

- ~~**Promote agent-memory feedback into Hallucinote product-level affordances (CLAUDE.md, skills, docs, generator API).**~~ **RESOLVED (W17 2026-05-20)** — all four memory rules promoted: CLAUDE.md "Hallucinote Behavioral Norms" appendix (stop-less, creative-vs-planning, sound-is-composition, microtiming-is-authorship), `/song-new` mode detection rewrite, `/song-pick-instruments` chain rewrite, `docs/song-authoring-conventions.md` sound-is-authorship + per-part-feel sections, `docs/snapshot-schema.md` multi-device chains subsection, generator `feel` kwarg on 11 helpers. Closed by W17-A/B/C/D/E. Original framing kept below for archaeology. Four feedback memories accumulated during punk-fate 2026-05-20 capture composition/workflow norms that today live only in agent state (`~/.claude/projects/.../memory/feedback_*.md`). Each is right *for this user with this agent*, but agent-memory is per-user, per-machine, can be `/clear`-ed, and vanishes when the user switches agents. If these norms are correct for Hallucinote *as a product*, they belong in the product's docs, skill bodies, and APIs so every user / future agent / human collaborator naturally does the right thing without depending on memory recall. The promotion targets:

  - **`feedback_stop_less.md`** (stop only on high-stakes decisions or must-answer questions) → CLAUDE.md "Doing Tasks" section gets a "Don't checkpoint after every workflow step" rule naming the threshold (high-stakes decision OR must-answer question). Skills ending in a "Final report to user" block (`/song-new`, `/ableton-push`, etc.) rewrite that block to be a status update, not a permission prompt — and default to continuing through the rest of the workflow when no real decision is pending.

  - **`feedback_creative_prompts_want_finished_deliverables.md`** ("make me X" means ship X) → CLAUDE.md's "Before Building: Requirements Clarity" section gets a "creative product prompt vs planning prompt" distinction so the agent recognizes the form. `/song-new` SKILL.md prelude names the two legitimate modes ("scaffold a frame for me to compose into" vs "make me song X") and routes accordingly — today it defaults to scaffold-then-stop, which is wrong for the README's flagship example. Overlaps with the existing `/song-new should drive end-to-end` backlog item — same skill rewrite carries both.

  - **`feedback_sound_is_composition.md`** (device chains ship in the snapshot; no mix-time todo) → `docs/song-authoring-conventions.md` gets a "Sound design is part of authorship" section. `docs/snapshot-schema.md` foregrounds the multi-device chain shape that already exists in the schema but isn't prominently surfaced. `/song-pick-instruments` SKILL.md treats picks as *chains*, not instruments. Overlaps with the existing `/song-pick-instruments picks device CHAINS` backlog item.

  - **`feedback_microtiming_is_authorship.md`** (feel goes in the part, not a humanize pass) → `docs/song-authoring-conventions.md` gets a "Per-part feel" section. The acceptance shape is **"punk kick and snare, lazy bluegrass guitar"** — kick+snare tight, guitar dragging, two coexisting feels in the same section, model-interpreted per part. **Surface this as principle, not registry.** No genre→groove table; no "every instrument in this section shares the same feel" assumption; no inherited section-level groove. The generator-API change (per-helper `feel` kwarg) is tracked separately in the dedicated microtiming backlog item below.

  **Why this matters:** agent memory is brittle (per-user, per-machine, lost on `/clear` or agent switch). Product-level affordances persist across users, agents, and time. A new collaborator (human or AI) using Hallucinote should arrive into a system that already nudges them toward "sound is composition" and "feel goes in the part" — they shouldn't rediscover these by trial-and-error and accumulate memory the way this session did. The four agent memories stay (useful as agent-state reinforcement), but the load-bearing canon is the product.

  **Sized:** medium-large. Concrete chunks, in landing order: (1) CLAUDE.md edits — cheap, lands immediately, establishes the behavioral norms; (2) `docs/song-authoring-conventions.md` "Per-part feel" + "Sound design is authorship" sections — frame as principle + small examples, no registries; (3) `docs/snapshot-schema.md` foregrounding device chains; (4) `/song-new` + `/song-pick-instruments` skill rewrites — these overlap with two existing top-of-backlog items, do them together. The generator `feel` API is its own item below. Caveat carried from the user's microtiming warning: be careful not to over-specify — e.g. do not seed `song-authoring-conventions.md` with a per-genre microtiming table; show *examples* the LLM can riff on, not *rules* it must follow. (punk-fate retrospective 2026-05-20)

- ~~**`/song-pick-instruments` should pick device CHAINS, not just instruments.**~~ **RESOLVED (W17-C 2026-05-20)** — `/song-pick-instruments` SKILL.md rewritten to pick device chains (instrument + post-FX + initial sends), with per-role default chain shapes for drums/bass/lead/pads/vocals and chain order loaded device-by-device. Step 5 writes `decisions/NN-signal-chains.md` after chain confirmation. For genre/style-driven prompts ("real punk energy"), the chain processing (Saturator on guitar, Drum Buss + Saturator on drums, tight envelope releases on synth leads) is what makes the sound credible — not the instrument alone. Today the skill terminates at one device per track (the instrument); effects are deferred to a "mix-time follow-ups" list the user is expected to do manually. punk-fate 2026-05-20 showed this fails: stock-Live Operator-based "Power Chords Guitar" alone reads as new wave, not punk; sampled bass alone reads as bass-line-demo, not punk-rock-bass; etc. Fix: extend the skill to write `devices[]` arrays carrying the instrument + the genre-appropriate effect chain into the snapshot. The LLM picks the chain shape per genre (cf. `feedback_prefer_llm_over_deterministic_module.md` — don't build a deterministic genre→chain registry; let the agent reason). Add a `decisions/NN-signal-chains.md` artifact per song so the choices are documented. Sized: medium — skill rewrite + snapshot validator pass (does the chain land + apply correctly via the device-phase loader). Related: see `feedback_sound_is_composition.md` in memory. (punk-fate reflection 2026-05-20)

- ~~**`/song-new` should drive end-to-end for creative product prompts.**~~ **RESOLVED (W17-B 2026-05-20)** — `/song-new` SKILL.md prelude now has explicit mode detection (`make-me-X` vs `scaffold-only`); make-me-X mode drives compose-and-mix through to a pushed song, scaffold-only mode terminates after scaffolding. Final report is mode-aware. The current `/song-new` workflow terminates at "scaffold + decisions + instrument-picks," with composition, sound design, and mix as separate follow-up sessions. For prompts of the form "make me song X" (the canonical creative product request — also the README's flagship example), this is the wrong termination point: the user expects to *hear* X on push, not get a scaffold + a checklist. Today the agent must chain past the skill's "Final report to user" block manually, and at each implicit boundary risk stopping unnecessarily. Two fixes: **(a)** the skill body's "If the user signaled they want to keep going on composition right now" branch should be expanded into a full compose-and-mix pass, default ON for creative product prompts and OFF for "scaffold a frame for me to compose into" prompts (which is the *other* legitimate use case); **(b)** the skill remains scaffold-only and a new `/song-make` or composite skill chains scaffold → pick-instruments → push → compose → mix-and-master. (a) is simpler. The user explicitly named this gap in the punk-fate retrospective ("agent didn't autonomously get to mixing and production"). Cross-link to `feedback_creative_prompts_want_finished_deliverables.md` and `feedback_stop_less.md` in agent memory. Sized: medium — skill rewrite + clear naming convention for "scaffold-only" vs "make-the-thing" prompts so users can pick. (punk-fate reflection 2026-05-20)

- ~~**Pattern helpers need a per-part `feel` parameter — not a post-hoc `/clip-humanize` pass, and not a shared genre/groove table.**~~ **RESOLVED (W17-E 2026-05-20)** — `feel: Mapping[float, float] | None` kwarg added to 8 drum helpers + aggregate, 3 bass helpers, and harmony tresillo_pluck. `apply_feel` helper + `Feel` alias in `primitives.py`. Per-part-per-call granularity (verse vs chorus can carry different feel dicts on the same generator); no shared groove instance, no inherited section-level groove, no genre registry. 9 new tests cover none-preserves-canonical, swing-eighths, multi-feel-same-section, per-clip verse-vs-chorus. Today helpers like `_punk_drum_bar` in songs/punk-fate/build.py emit notes on the exact 8th-note grid. The `/clip-humanize` skill jitters velocity per-clip after-the-fact — useful for breaking obvious robot-grids on already-good parts, but NOT a substitute for writing parts with feel. punk-fate 2026-05-20 user feedback was unambiguous: "feels quantized — build.py should bake in good microtiming adjustments appropriate for the song, coordinated across instruments. We shouldn't push wrong timing then clean up." Fix: add a `feel` parameter on pattern helpers (e.g. `drums.tresillo_hats(bars=4, feel='lazy back-half')`) accepting **freeform string OR structured microtiming offsets**. The LLM passes per-part feel descriptions; the helper applies offsets. The user-named acceptance case is *"punk kick and snare, lazy bluegrass guitar"* — kick+snare tight, guitar dragging, two coexisting feels in the same section. Coordination across instruments (kick+bass lock-up when that's the intent) is an **emergent property of the LLM choosing consistent feel values per part**, NOT a structural constraint that forces homogeneity. **DO NOT build:** a fixed genre→groove registry, a shared groove instance per section, a `groove` parameter at the song or section level that forces every track to inherit. The `feel` parameter lives per-helper-call, no inherited defaults. Cf. `feedback_prefer_llm_over_deterministic_module.md` — the LLM owns the interpretation, no deterministic groove math. Sized: medium — per-helper `feel` kwarg, freeform-string parser (or just pass through to a microtiming applicator), tests covering the multi-feel case ("kick tight + guitar dragging in same section"). Cross-link: top-of-backlog promotion item covers the docs / skill / conventions surfacing; this item is the code-level generator API change. (punk-fate reflection 2026-05-20, framing refined 2026-05-20 after user warned against over-specification)

- ~~**Drum Rack pad-mapping discovery — don't assume GM-derived mapping.**~~ **RESOLVED (Arc 1 / A3, 2026-05-21)** — see the Hot Rod Kit entry at the top of the backlog for the full resolution. `push_cli execute` auto-populates `drum_pad_mappings` after the devices phase; `Kit.from_device` reads kit-specific notes; `Kit.pitch_of` raises on the wrong-sound case (GM-default-collides-with-different-chain); `Kit.try_pitch_of` / `assert_has` are the safe alternatives. Original framing kept below for archaeology. punk-fate 2026-05-20 wrote `KICK=36, SNARE=38, HAT_CLOSED=42, HAT_OPEN=46, CRASH=49, TOM_LO=43, TOM_MID=47, TOM_HI=50` based on General MIDI convention and hoped Hot Rod Kit followed it. The kit's actual pad mapping was never probed — if any pad lands on a different sample slot (e.g. CRASH at 51 instead of 49), the part plays the wrong sound. Fix: extend the device-phase loader's post-load step to probe each loaded Drum Rack's chain names + key ranges (Ableton exposes `chain.note_key_range` per drum chain) and persist them into the DB; `build.py` reads canonical names ("Kick", "Snare", "Closed Hat") and the helper resolves them to whatever MIDI note the loaded kit actually maps. This makes drum parts kit-portable in the way the snapshot's `preset_query` already makes instruments portable. Sized: medium — capture-side probe + DB shape + helper lookup API. Lower priority than the three items above — current GM-assumption works for most Live stock kits. Stash here so we don't forget. (punk-fate reflection 2026-05-20)

- **Push-state coherence — three pieces of per-machine state with independent invalidation rules and no consistency check.** The push orchestrator carries state across three locations: (1) `/tmp/ableton-push-snapshot.json` (filesystem; the probe of Live's current track/return shape), (2) `ableton_sessions` rows (DB; the binding between this DB and a Live set), (3) `ableton_links` rows (DB; mappings from DB ids to Live indices). Each has its own lifecycle. None checks whether the others are still consistent with the others or with Live. Three drift bugs hit during punk-fate 2026-05-20 are the same architectural smell expressed three ways, not three independent fixes:

  - **Stale snapshot file.** `/tmp/ableton-push-snapshot.json` from a prior push persists across runs. Second push reads it and gets the wrong tracks/returns shape. Repro (punk-fate first push): would have created duplicate song tracks at Live indices 5-8 alongside the existing 5-8 (12 tracks total) if not caught by manual re-probe. Per-symptom fix: orchestrator owns the probe — e.g. a `--probe-from-live` flag on `push_cli probe-and-link` so the CLI invokes MCP directly to refresh rather than trusting the file.

  - **`build.py --reset` wipes `ableton_sessions` + `ableton_links`.** Running `--reset` between pushes drops the DB and rebuilds it, taking the sessions + links rows with it. Repro (punk-fate compose-iterate): first probe-and-link mints session `f3f1...`; user runs `python songs/punk-fate/build.py --reset` to repopulate from the edited compose-half; second `execute f3f1...` errors immediately: *`push_cli: no ableton_sessions row with id 'f3f1...'`*. The session_id is presented as a durable handle but doesn't survive the natural compose-half iteration loop. Per-symptom fix: narrow `--reset`'s scope to song-content tables (clips, notes, arrangements, events log); preserve sessions + links + device-links.

  - **Stale `ableton_links` after Live track deletion.** When the user deletes a Live track that was previously linked, the link row remains. Re-running probe-and-link upserts new matches but doesn't delete stale ones. Repro (punk-fate post-cleanup): linked song tracks → Live indices 5-8 → user deletes those Live tracks → re-probe-and-link correctly reports `unmatched_db_tracks` but leaves the stale links in place; `tracks` phase skips as "already linked"; `clips` phase fails 28/28 with `IndexError: track_index N out`. Recovery required `--auto-session` to mint a fresh row. Per-symptom fix: probe-and-link reconciles strictly against the snapshot, deleting stale link rows whose `ableton_index` no longer appears.

  **Structural fix candidates (one of these resolves all three; per-symptom fixes serially leave the underlying smell in place):**

  - **(A) One canonical entrypoint owns all three pieces of state.** `/ableton-push` (or the underlying CLI) probes Live, mints the session if needed, reconciles links — in that order, every time — before executing. No tmp file persists between runs; no stale links survive a probe; the session row is created on demand. Skill becomes probe-driven, not state-driven. Sized: medium — skill body + CLI rework.
  - **(B) Coherence-check layer between probe-and-link and execute.** Before `execute` runs, validate: (1) the session row exists; (2) every link's `ableton_index` exists in a freshly-probed Live snapshot; (3) the snapshot file's tracks list matches the live probe. Refuse-and-teach on any mismatch with a clear recovery path. Sized: small — pure validation layer; per-symptom fixes become optional after this lands.
  - **(C) Per-symptom fixes (the three above) plus a session-handoff doc.** Land them serially. Sized: small × 3, but cumulative cost exceeds (A) or (B) because each fix leaves the smell in place and a fourth state-drift bug will surface in the next session.

  Recommendation: (A) is the right shape long-term; (B) is the cheapest defense-in-depth that buys time for (A). The cross-cutting nature was named by the user during the punk-fate retrospective: "three pieces of per-machine state with three independent invalidation rules and no shared 'is this state still consistent with Live?' check." (punk-fate test session 2026-05-20; consolidates three previously-separate backlog items)

- **Future sibling skill: `/song-import` — ingest an existing Ableton Live set into a new Hallucinote song dir.** Sibling to `/song-new`. `/song-new` scaffolds from templates (no Live required); `/song-import` would capture an open Live set + pull notes / arrangement / envelopes into a fresh DB + generate a build.py thin wrapper. Today the agent can do this manually by chaining `tools.scaffold_song` + `tools/capture.py` + `/ableton-pull` against an empty DB, but the loop isn't quite right because `/ableton-pull` is built for state diffs on an existing DB, not first-ingest of clips/notes/arrangement. The full path needs a "pull first-time everything" path (gated on the existing pull-side scope items listed at the bottom of this backlog). Naming chosen to match the `<scope>-<action>` convention established in the skills namespace cleanup (2026-05-20). (skills-replace-prompts refactor, 2026-05-20)


- ~~**`/new-song` SKILL.md step 6 asks the assistant to do something the harness doesn't expose.**~~ **RESOLVED ARCHITECTURALLY (2026-05-20)** — the entire class of MCP-prompts-as-agent-callable went away. The 7 prompts (`pick_instruments_for_song`, `start_new_song`, `create_midi_track_with_instrument`, `setup_sidechain_compression`, `build_return_bus`, `humanize_clip_velocity`, `compose_section_pattern`) were migrated to Claude Code skills (`.claude/skills/song-pick-instruments`, etc.); `prompts/__init__.py` deleted; MCP server PRIMER no longer advertises a `prompts/get` path; `/song-new` SKILL.md step 6 now invokes `/song-pick-instruments`. Sibling skill renames landed in the same pass: `new-song`→`song-new`, `ableton-install-mcp`→`ableton-mcp-install`, `ableton-uninstall-mcp`→`ableton-mcp-uninstall`. (closed by skills-replace-prompts refactor)
- ~~**Scaffold template should use stripped return names (`Reverb` / `Delay`, not `A-Reverb` / `B-Delay`) in `captured_session.json`.**~~ **RESOLVED (verified W17 2026-05-20)** — verified `tools/templates/song/captured_session.json.tmpl` already ships stripped names (`Reverb` / `Delay`). Item closed; surrounding scaffold context kept for archaeology. `tools/templates/song/captured_session.json.tmpl` ships `A-Reverb` + `B-Delay` as return names. Every `python songs/<slug>/build.py` from a fresh scaffold then trips `replay_capture`'s W4-C strip warning (added in the v1 polish bundle): *"stripped Live's `<letter>-` slot prefix from 2 return name(s): 'A-Reverb' -> 'Reverb', 'B-Delay' -> 'Delay'..."*. The warning is correct (the DB stores stripped names per W4-C convention) but the noise is gratuitous on a clean first build — the scaffold should ship the stripped form. Fix: edit `tools/templates/song/captured_session.json.tmpl` to use `"name": "Reverb"` + `"name": "Delay"`. Sized: 1-line template edit + verify the existing `replay_capture` test still passes. Symmetric concern: regenerate `songs/falling-walking/captured_session.json` if it has the same convention drift (low-risk read; not blocking). (neon-feedback test session 2026-05-20)
- ~~**W10-E2 measured impact: per-clip-with-inline-notes agent dispatch is the test-iteration ceiling.**~~ **RESOLVED (W10-E2 2026-05-20)** — landed as `push_cli execute` subcommand on `feat/w10-e2-push-execute`. The prior "Python-side MCP client access not available" framing was a misread: we needed Live client access (`hallucinote_mcp.client.send`), which is already public-ish — the MCP server is just a thin protocol bridge over the same TCP client. `execute` walks all ten phases in-process and dispatches every call directly against Live's Remote Script over TCP; bytes never enter the agent's context. Phase-bounded error accumulation halts cleanly at phase boundaries; `.last-push-state.json` + `.last-push-errors.json` give the agent structured forensics without reintroducing inline notes payloads (`args_summary` strips large arrays to counts). 11 new tests; design at `.prawduct/artifacts/push-execute-design.md`. (closed by W10-E2)
- **`ableton_track(action='delete')` on the last remaining track should refuse-and-teach, not surface Live's bare `RuntimeError`.** Live requires at least one track in a set and rejects the delete of the last surviving track with `RuntimeError: Couldn't delete track.` from the LOM. The MCP wraps that as a generic error with `hint: "Check ableton_track(action='help') for the action's preconditions and value ranges."` — but `help` doesn't surface the "Live requires ≥1 track" precondition either. Fix: handler-side precondition check before calling `Song.delete_track()` — if `len(song.tracks) <= 1`, raise a teaching error pointing at the Live constraint. Same shape likely applies to `delete_scene` on the last scene (verify). Sized: ~10 LoC handler + 1 unit test (mocked Song with 1 track) + help-text update. (neon-feedback test session 2026-05-20)
- **Clean-default-scaffold workflow must handle Live's "≥1 track" constraint.** When `/ableton-push --auto-session` cleans the default `1-MIDI`/`2-MIDI`/`3-Audio`/`4-Audio` scaffold (the feature item above), naively deleting all four fails on the fourth delete (per the entry above). Two viable workflows: (a) delete N-1 defaults, then rename the last to absorb one of the song's DB tracks (probe-and-link rerun picks it up automatically); (b) push the song first (which creates the song's own tracks), then delete the originals that are now safe to remove. (b) is simpler to implement and reason about. The skill should adopt one path explicitly rather than naively iterating delete calls. (neon-feedback test session 2026-05-20)
- ~~**First push of a new song should offer to clean Live's default scaffold tracks.**~~ **RESOLVED (R-1.2 2026-05-20)** — `push_cli cleanup-default-scaffold` subcommand bundles the detect → confirm → descending-delete → re-link flow into one atomic call. W18-D already shipped the detection (`default_scaffold_unmatched_tracks`); R-1.2 ships the cleanup execution. Original framing kept below for archaeology. **First push of a new song should offer to clean Live's default scaffold tracks.** Live's brand-new-set default is always `1-MIDI` / `2-MIDI` / `3-Audio` / `4-Audio` + `A-Reverb` / `B-Delay`. When `/ableton-push --auto-session` runs (first push for this song's session), probe-and-link surfaces those default tracks as `unmatched_live_tracks` and the W12-C confirmation gate fires — asking the user to confirm pushing *alongside* the default scaffold. But "alongside" is almost never what the user wants: they want neon-feedback's tracks to be the only tracks. Fix: when `auto_session_created==true` AND every `unmatched_live_tracks[]` entry matches the canonical default-scaffold name set (`{1-MIDI, 2-MIDI, 3-Audio, 4-Audio}` exactly, or any subset thereof), have the skill offer "delete the default tracks first?" as the default answer to the confirmation prompt rather than just "continue alongside." Same for returns (`A-Reverb`/`B-Delay`) when they're unmatched — but in practice returns are usually reused (the song's `Reverb`/`Delay` strip the prefix and match), so the tracks case is the load-bearing one. Implementation: extend the `/ableton-push` skill body's Step 1 logic, OR add an `--auto-clean-defaults` flag to `push_cli probe-and-link`. Sized: ~20 LoC SKILL.md + 1 test scenario. (neon-feedback test session 2026-05-20)
- ~~**`/new-song` skill should surface `pick_instruments_for_song` as the immediate post-scaffold step.**~~ **RESOLVED (2026-05-20)** — `/song-new` (renamed from `/new-song`) step 6 invokes `/song-pick-instruments` directly (migrated from the `pick_instruments_for_song` MCP prompt). The final-report block points the user at the same skill as the first next step. (closed by skills-replace-prompts refactor)
- **`docs/snapshot-schema.md` should surface "load post-push + recapture" as the canonical built-in-preset workflow.** The doc currently says *"hand-authoring URIs is unreliable (Wave 0 spa-7c). Prefer probing via `ableton_browser` first."* That tells you what NOT to do but doesn't name the *right* workflow for the "I want this specific drum kit / preset" case — which is: push with an empty device of the right class, use `pick_instruments_for_song` (or direct `ableton_browser` + `ableton_device(action='load')`) to install the kit by name post-push, then run `tools/capture.py` so the URI lands in the snapshot for next time. Sized: ~30 LoC docs edit adding a "Picking built-in content presets" subsection between "Device chain shape" and "What `replay_capture` doesn't ingest". Cross-link from the `/song-new` and `/song-pick-instruments` skills. (neon-feedback test session 2026-05-20)
- **Compose-time built-in content selection by name (snapshot-author-time, no Live required).** Today picking a drum kit / instrument preset by name requires Live to be running (URIs come from Live's live browser; FileIds are per-machine — Case A). For strict-portability Live built-ins ("Kit-Core 909", "Late Nite Kit", "Bass-Pluck", etc.) the kit identity IS stable across machines — only the FileId catalog handle differs. A snapshot-level reference like `"preset_query": "Drums/Kit-Core 909"` that the push planner resolves at push time (via the same browser query path) would let composers express kit picks offline, without machine-specific FileIds, and without the awkward push-then-recapture loop. Strict-mode-only (the loader refuses on unresolvable queries, no fuzzy matching). Sized: needs snapshot-schema field + push planner resolver + tests; medium chunk. v1.0 follow-up — depends on `ableton_browser(action='search')` (the same dependency as W13-A). (neon-feedback test session 2026-05-20)

- **W13-B follow-up: extract shared plugin-discriminator into a single module.** Both `src/hallucinote/sync/compat.py:_PLUGIN_CLASSES` + `_is_plugin_class()` and `hallucinote_mcp/.../handlers/device.py:855-863` (`is_third_party_plugin`) implement the same logic (explicit set + `"Plugin" in class_name` substring). They're kept consistent today by a lock-test (`test_plugin_classes_lock_matches_mcp_side` + `test_classify_device_substring_branch_routes_to_third_party`) — sufficient for v0.9, but a drift-prone shape long-term. Natural home: W11-A's `hallucinote-core` shared package, where the discriminator becomes one source of truth that both `hallucinote-mcp` and `hallucinote.sync.compat` import. Defer until W11-A's extraction lands so the move happens once rather than twice. (v0.9.0 cumulative Critic note + PR reviewer note 3, 2026-05-20)
- **W13-A v1.0: instrument fallback identity (Case A — same plugin, different catalog id).** v0.9 mitigation via W13-B compat check at least flags missing plugins; Case A (plugin IS installed but Live's FileId differs across machines) still falls through. v1.0 design: snapshot records `(class, display_name, manufacturer, pack_name, params_dialed)`; push tries FileId → falls back to `ableton_browser(action='search', name, class)` → re-applies params after load. **No longer blocked** — Sweep 0 (browser-search-and-checklist branch) ships `ableton_browser(action='search')`. W13-A is now scopable for v1.0; depends on the new search action landing on develop. (W13 wave plan; carried forward from v1 plan; unblocked 2026-05-20)
- **falling-walking: rename `tests/test_build.py` → `tests/test_falling_walking_build.py`.** Per the v1 polish bundle's new `pyproject.toml` comment block (lines 31-37), per-song convention is `test_<slug>_build.py` to keep filenames unique under `pytest -n auto --dist loadgroup`, citing falling-walking as the pattern to follow. But falling-walking's own bootstrap file is still bare-named `test_build.py` — the very pattern the comment says NOT to follow. Scaffold tool already renames correctly for new songs, and the other three canary songs follow the convention. No collisions today (only one bare `test_build.py` in the tree), so hygiene only. Sized: one `git mv` + verify no broken references. (V1 cumulative PR reviewer note 2, 2026-05-20)
- **W10-A follow-up (C1): extend `probe_and_link` to bind devices by class + chain position.** W10-A closed the arrangement-side duplication and verified the returns-side slot-prefix path. The C1 device-side equivalent — when Live's pre-existing devices (e.g., default `A-Reverb`'s built-in Reverb) match DB devices but aren't yet in `ableton_links`, `_emit_device_calls` emits a `load` and duplicates the device — needs `probe_and_link` to also probe device chains per linked track/return and auto-bind by `(position, class_name)` so subsequent `_emit_device_calls` skips correctly. Plumbing: extend `probe_and_link`'s signature to accept `live_devices_by_parent: dict[(parent_kind, parent_index), list[dict]]` (each entry = `{position, class_name, display_name}`); extend `push_cli.probe_and_link` to query device chains per linked track/return; match algorithm = (position, class_name) equality with a display-name tie-break. Sized: ~120 LoC + 4-6 tests. Defer until either a v1.1 canary measures the workaround pain OR `R-1` cumulative Critic flags it. v1 mitigation: the existing `unmatched_live_tracks/returns` confirmation gate (W12-C) covers the related "another song's tracks/returns" scenario; the C1 case (a song pushed onto Live's *default scaffolding*) survives this gate because the defaults look like a "fresh" set. (W10-A follow-up 2026-05-20)
  - **Second-occurrence repro (punk-fate 2026-05-20)** — hit the same C1 shape but via a different trigger: a `build.py --reset` between two pushes. First push loaded Beastly Kit / Saturated Bass / Guitar Electric Clean / Noisy Pad onto Live tracks 5-8 cleanly (devices phase `6/6 ok`); user requested a compose-half edit; rebuild via `build.py --reset` minted fresh DB device IDs; second `push_cli execute` ran the devices phase and called `ableton_device(action='load', track_index=5, kind='DrumGroupDevice', preset_query={...Beastly Kit...})`. The `browser.load_item` invocation didn't append (Live silently no-op'd because a Beastly Kit was already at position 1), and the runtime check `device_count_after == device_count_before` tripped. Error surfaced as: *`RuntimeError: load: Live did not append a device on track 5 after browser.load_item; the item may not be loadable on this parent (e.g. instrument on a return)`*. Two follow-ups for this entry: **(i)** ~~the "instrument on a return" hint in that error is misleading for the `--reset` repro — the actual cause is "device of matching class+name already present on parent" — when the load fails the no-append check, the error should surface what *is* already on the parent (list `class_name`s of existing devices at that position) so the diagnose-and-fix loop doesn't need a separate `ableton_device(action='list')` probe to figure out the conflict.~~ **RESOLVED (Arc 1 / A2-resid, 2026-05-21)** — `device.py::load_handler` now enumerates `fresh_parent.devices` on the no-append failure and includes the existing chain `[index:class_name, ...]` in the error message. The "instrument on a return" hint is preserved only for return-parent calls (where it's actually structural). **(ii)** workaround verified — synthesizing apply results (`[{"ok": true, "result": {"device_index": 1}}, ...]`) via `push_cli plan devices > plan.json` + `push_cli apply ... --plan plan.json --results results.json` writes link rows for the existing devices and lets a subsequent `execute` skip the devices phase idempotently. **Largely subsumed by W20-A's `_match_devices_for_linked_parents`** (probe-and-link --probe binds existing devices via `(parent, position, class_name)` matching, no hand-plumbed JSON needed) — kept on the backlog as a documented escape hatch for cases where the bind heuristic fails. (validates C1's urgency: hit twice in two days, by two distinct triggers — defaults-scaffold path AND `--reset`-then-repush path; bump priority accordingly.) (punk-fate test session 2026-05-20)
- **W10-F follow-up: `_track_kind` (mutations.py) + `_track_kind_for_envelope` (push.py) duplicate a one-row SELECT.** `Q.get_track` already exists in `queries.py`. The two helpers live one layer apart by design (DB-layer mutator vs sync planner safety-net) so layer boundary is explicit, but the SELECT itself could route through `Q.get_track` to avoid having two places to change if the `tracks` table grows columns. Sized: ~10 LoC + drop both helpers. Defer until either layer changes. (W10-F Critic note 1, 2026-05-20)
- ~~**W12-B follow-up: `capture_plan()` still emits pre-rename probe names**~~ **RESOLVED (v1 polish bundle 2026-05-20)** — `capture_plan()` strings + module docstring + `tools/capture.py` mirror retargeted to `ableton_return(action='list')` / `ableton_track(action='get_info'|'get_sends')` / `ableton_device(action='get_parameters')`. `test_capture_plan_lists_expected_probes` pinned to new names. (W12-B PR review note 2; closed by v1 polish bundle)
- **W12-B follow-up: `_MIXER_FIELDS` / `_RETURN_FIELDS` carry both `panning` and `pan` defensively.** No probe emits `pan` today — `_norm_pan()` documents the convention as `panning`. If a future probe ever emits `pan` alongside a snapshot's existing `panning`, `diff_snapshots` would surface both as spurious add/remove rather than as identity. Either drop `pan` from the field tuples and rely on the established invariant, or add an alias-normalization step before diffing. Not a behavioral defect today. (W12-B PR review note 3, 2026-05-20)
- **Native Linux support — gated on Ableton shipping a Linux build.** v1 documents Linux as unsupported (README + install skill warn-and-confirm); the existing `install_paths.candidate_user_libraries()` Linux branch (`~/Ableton/User Library`) is a Wine / CrossOver best-guess. If Ableton ever ships native Linux, the install skill needs: (a) a real Linux preferences root (today `_live_preferences_root()` in `hallucinote_mcp/install_paths.py` returns `None` on Linux, so `installed_live_versions()` always reports `[]`); (b) a `live_is_running()` Linux branch (likely `pgrep -i -f 'Ableton Live'` — same shape as the macOS branch); (c) a `live_log_path()` Linux mapping; (d) a decision on whether the Wine / CrossOver fallback path stays as a secondary candidate. Until that day, Wine users continue to get the warn-and-confirm and the existing graceful degradation. (W15-C 2026-05-19)
- **Derived-views drift: `views_enabled: true` but no `chunks=NN | status=shipped | release=... | scope=...` tags exist in `.prawduct/artifacts/change-log.md`, and `python3 tools/product-hook regen-views` errors with `ModuleNotFoundError: No module named 'lib'` (`tools/lib/` doesn't exist).** Recurring on every PR review since W12-A. Two paths: (a) backfill a baseline tagged change-log entry per shipped chunk and fix the `lib/` import — most likely belongs in the upstream `tools/product-hook` framework sync the user is authoring; (b) flip `views_enabled: false` in `.prawduct/project-state.yaml` for now. Either path closes the gate; current state is "view derivation declared but inert." (W10-B/C/D + W12-C/W15-D + W15-B Critic notes, 2026-05-19/20)
- **Hook: auto-bump `.prawduct/.test-evidence.json` git_sha field on commit.** Across 6 chunks this session, every PR reviewer flagged stale `git_sha` in test-evidence — `product-hook test-status` checks timestamp freshness, not SHA pointer, so the staleness slipped through. A post-commit hook (or a pre-commit reminder) that re-stamps `git_sha` from `HEAD` would zero out this class of friction. Sized: ~10 LoC in tools/product-hook. (V1-push session reflection 2026-05-19)
- ~~**W10-E2 (deferred from W10-E): streaming `execute` subcommand for push_cli.**~~ **RESOLVED (W10-E2 2026-05-20)** — shipped as `push_cli execute` directly against Live's Remote Script (no streaming protocol needed). See the closed entry above for the design correction. (closed by W10-E2)
- **W12-A edge case: `delete_notes` doesn't set events.clip_id when an LLM removes the last notes from a build-owned clip.** `delete_notes` (in `db/mutations.py`) emits NOTES_DELETED with `clip_id=None` because the operation is multi-clip in shape (`affected_clips` lives in the payload). Consequence: a build-owned clip whose only LLM-touch was a `delete_notes` call has no events.clip_id row for it, so `_latest_actor_for` (clip path uses events.clip_id) doesn't see the LLM as the latest actor — the clip becomes tombstone-eligible on the next build. Workaround for users: LLM edits should prefer `replace_clip_notes` (which sets events.clip_id correctly). Fix candidates: (a) emit one NOTES_DELETED event per affected clip with clip_id set, (b) set clip_id when `len(affected_clips)==1`, (c) extend the clip-tombstone-actor lookup to also scan payload's affected_clips. (W12-A PR review re-review residual, 2026-05-19)
- **W12-A v1.1 hardening: nested rack chains aren't enumerated by tombstone walker.** `_tombstone_untouched` (in `db/mutations.py`) joins device_chains/devices/device_parameters through `parent_track_id`/`parent_return_id` only; chains parented via `parent_rack_device_id` survive unless their parent rack device is itself tombstoned. Falling-walking has no nested racks today, so v1 isn't hurt; v1.1 should add a recursive walk (or schema-level cascade trigger) once nested-rack push/pull ships. (W12-A Critic chunk note 3, 2026-05-19)
- ~~**Wave 0 / PR review: stale `_TRACK_LEVEL_GAP_HINT` text + module docstring in `hallucinote_mcp/.../handlers/automation.py`.**~~ **RESOLVED (W10-F 2026-05-20)** — both rewritten to "session only — `duplicate_to_arrangement` carries the envelope into arrangement." Two regression tests assert the hint includes "duplicate_to_arrangement" and does NOT include the old "(or session)" parenthetical. (Wave 0 PR review 2026-05-19; closed by W10-F)
- **Wave 0 / D1 v1.1: planner auto-partition envelopes across per-section session clips.** v1 ships refuse-with-teaching (W10-F) for long envelopes whose range exceeds any single session clip. v1.1 enhancement: planner detects the multi-clip case, splits the DB's logical envelope at session-clip boundaries, emits one sub-envelope per covering session clip; pull stitches adjacent identical envelopes back into the one logical envelope. Natural Hallucinote shape (DB models one user-visible curve, Live realizes it across the clips that exist). Requires both push-side split + pull-side stitch + round-trip test coverage. (Wave 0 triage Group D 2026-05-19)
- **Wave 0 / D3 v1.1: mixer envelopes on audio tracks via audio-clip DB model.** Gated on `scope.later` "audio clips: clip kind discriminator, file references, warp metadata" landing in the DB. Once audio session clips are addressable in Hallucinote, the existing envelope-emitter family can host mixer/send envelopes on audio session clips the same way it does for MIDI session clips. v1 ships refuse-with-teaching (W10-F); v1.1 enables the actual envelope path. (Wave 0 triage Group D 2026-05-19)
- ~~**Wave 0 paper-cut: `replay_capture` silently strips `[A-Z]-` slot prefix.**~~ **RESOLVED (v1 polish bundle 2026-05-20)** — `replay_capture` now emits a one-per-call `UserWarning` summarizing the strips; `docs/snapshot-schema.md` gained a "Return names: stored stripped" callout under the returns section; `strip_return_slot_prefix` docstring points there. 2 new tests pin the warn-fires + no-warn-when-no-strip behavior. (Wave 0 canary `solo-piano-ambient` runbook step 3; closed by v1 polish bundle)
- ~~**Wave 0 paper-cut: cues phase plan should warn about arrangement-extent dependency.**~~ **RESOLVED (v1 polish bundle 2026-05-20)** — `plan_push_cue_points` now compares each cue's `position_bar` against `max(arrangement_clips.end_bar)` and emits a plan-time warn for the violators (or a "no arrangement_clips" warn when the DB has no arrangement at all). 3 new tests cover: no arrangement → warn; arrangement to bar 17 + cue at bar 32 → warn names only the late cue + the gap; all cues within extent → no warn. (Wave 0 canary `full-band-rock` runbook step 7e; closed by v1 polish bundle)
- **Wave 0 important: device kind canonical mapping table + loader auto-fallback.** `ableton_device(action='load', kind='AnalogDevice')` is rejected by the loader even though `AnalogDevice` is Live's actual internal `class_name`. Other stock kinds work (`Operator`, `Compressor2`→`Compressor`, `Reverb`, `GlueCompressor`). No canonical mapping table from Live's `class_name` to the loader's accepted `kind` values. Fix: add a mapping table (or loader auto-fallback that retries with `name=class_name.removesuffix("Device")` if the `kind`-named browser node fails). Probably fits as a W13-A sub-task per Wave 0 triage Group G. (Wave 0 canary `full-band-rock` runbook step 7b, 2026-05-19)
- ~~**Wave 0 paper-cut: `hallucinote.tempo.to_live_bpm` helper.**~~ **RESOLVED (v1 polish 2026-05-20)** — `src/hallucinote/tempo.py` ships `to_live_bpm(pulse_bpm, pulse_kind, time_signature=None) -> float` accepting 12 pulse kinds (whole / half / dotted_half / quarter / dotted_quarter / triplet_quarter / eighth / dotted_eighth / triplet_eighth / sixteenth / dotted_sixteenth / triplet_sixteenth). Refuses unknown kinds with the accepted list. `time_signature` is reserved-but-unused (pulse-to-quarter ratio is meter-independent today). Documented in `docs/song-authoring-conventions.md`. 14 new tests; suite 1496→1510. (Wave 0 canary `odd-meter-experimental` runbook step 7a; closed by v1 tempo helper)
- **Wave 0 paper-cut: cue-point auto-disambiguation for repeated section names.** Live's arrangement-locator UI lists cues by name; a song with 3 cues named `chorus` shows three ambiguous entries. Planner-side fix: when a song has cue-name duplicates, emit `chorus 1`, `chorus 2`, `chorus 3`. Or document the convention so authors handle it on the build.py side. (Wave 0 canary `full-band-rock` runbook step 3 + open Q7, 2026-05-19)
- ~~**Wave 0 important: MCP gap — `ableton_clip(action='read_notes')`.**~~ **DOC-RESOLVED (v1 polish bundle 2026-05-20)** — `ableton://guides/gaps` gains a "Partial gap" entry naming the missing one-off-read action and the ableton-pull workaround. The capability itself (`Clip.get_notes_extended()`) is used by the pull pipeline today; the interactive read action is still v1.1 scope. (Wave 0 canary `odd-meter-experimental` runbook step 7e; doc-closed by v1 polish bundle)
- **Wave 0 paper-cut: polyrhythm helper using `fractions.Fraction`.** `(7.0 / 5) * 3 / 2.0` yields `2.0999999999999996` (IEEE 754 sub-LSB drift) — the SQLite REAL column round-trips it faithfully, but the AUTHORING side introduces it without warning. A future `hallucinote.polyrhythm(n, against=k)` helper should compute offsets via `fractions.Fraction(against, n)` and float-convert only at the mutator boundary. Document the trap in `docs/polyrhythm-authoring.md` or annotate the future helper. (Wave 0 canary `odd-meter-experimental` runbook step 8, 2026-05-19)
- **Wave 0 important doc: `docs/song-authoring-conventions.md` covering repeated-section pattern.** Schema permits both "one clip, N arrangement placements" and "N clips, N placements" for a section that repeats (verse/chorus). The full-band-rock canary picked the second by guess; mutation semantics of the first are undefined ("if I edit the chorus melody, do all 3 placements update?"). Document the convention. Could also live as a W9-A artifact. (Wave 0 canary `full-band-rock` runbook step 3 + open Q1, 2026-05-19)
- **`_serialize_markdown` defensive: list items may contain `,` / `[` / `]`.** W8-B's `write_markdown_ref` calls `_serialize_markdown` to round-trip frontmatter through the YAML-subset parser. List items (`tags`, `related`, `bars`) get serialized as `[a, b, c]` without quoting. If a future tag or `related` path contains a `,` or `[` / `]`, the parser will silently split or fail to parse on the next reindex. Today's tags are slug-shaped so this isn't exercised, but the wrap is the LLM-facing surface. Defensive fix: either (a) quote list items containing those chars, (b) reject them at serialization time with a teaching error, or (c) switch to a multi-line list format in the parser. Sized as ~10 LoC + 2 tests. (W8-B Critic cumulative note 3, 2026-05-19)
- **W8-C framework-coupled wiring — session briefing + CLAUDE.md addendum for `/song-context`.** The Wave 8 plan named two targets for W8-C: (a) extend `tools/product-hook` so the session briefing surfaces in-scope song decisions + structural annotations; (b) add a CLAUDE.md addendum mirroring the existing `/learnings [topic]` guidance for `/song-context`. Both files are currently in the parked-upstream-framework set per project memory `project_prawduct_framework_authorship`. Adding hallucinote-specific behavior to those files would conflict with the in-flight upstream sync. W8-C shipped only the SKILL.md "When to invoke" guidance enhancement; the framework-coupled pieces are deferred until upstream sync settles. Then: (1) `product-hook` should detect "song in-scope" (any file touched in a `songs/<name>/` folder during the session) and inject a `Song context:` block with the song's most-recent 5 decisions + all `kind: structural-fact` annotations; (2) CLAUDE.md should add a line like "Before non-trivial composition, run `/song-context [topic]`" next to the existing `/learnings` guidance. (W8-C descope 2026-05-19)
- **Agent-side push/pull/capture skill wraps in `M.request(...)`.** W8-B (2026-05-19) shipped the request-lifecycle building blocks (`M.request` context manager, `M.close_request`, `kind` parameter on `create_request`, `MARKDOWN_REF_RECORDED` audit event). The Wave 8 plan named "wire push/pull/capture drivers" as a goal but those entry points are agent-orchestrated (the skills in `.claude/skills/ableton-push|pull` and the capture flow), not Python drivers — there's no Python function to wrap. The next move is to update the three skills' SKILL.md so they instruct the agent to wrap its mutator calls in `with M.request(conn, kind='push'|'pull'|'capture', actor='llm', ...)` so cycle metadata + nested events accumulate cleanly. Not blocking V1; mutator surface is in place. (W8-B 2026-05-19; Critic scope-truthfulness note resolved by descoping here)
- **`ableton_device(action='load', kind=...)` can name-match the wrong device class via Live's browser.** Empirical finding (W7-0 session 2026-05-19): loading `kind='Drum Rack'` actually loaded an `InstrumentGroupDevice` because Live's browser had a saved Instrument Rack preset named "Drum Rack" that matched before the canonical empty Drum Rack node. The load reported success at `device_index=N` but the resulting class was wrong. **Workarounds today:** use `preset_uri` for unambiguous loads (we used `query:Drums#FileId_5418` for Late Nite Kit successfully); verify `class_name` via `ableton_device(action='list')` after a name-based load. **Fix candidates:** (a) restrict the handler's display-name walk to NODES under the *canonical* category root (e.g., for `kind='Drum Rack'` walk only the drums root, not instruments); (b) prefer the empty-rack canonical URI when the name matches a built-in rack class; (c) surface the loaded `class_name` in the load response so callers can detect mismatches. (W7-0 session 2026-05-19)
- **`ableton_device(action='load', kind='Instrument Rack')` fails outright with no preset_uri.** Empirical finding (W7-0 session 2026-05-19): bare `kind='Instrument Rack'` returns `"Live did not append a device on track N after browser.load_item"` because the empty Instrument Rack node isn't directly loadable by display name from the instrument root (unlike Drum Rack + Audio Effect Rack, which DO have such nodes). **Workaround:** create an empty Instrument Rack by Cmd+G grouping selected devices in Live's UI, then probe via MCP. **Fix candidates:** (a) special-case "Instrument Rack" in the handler with the correct browser node lookup; (b) surface a teaching error pointing at the workaround when the bare name fails. Symmetric concern across rack subclasses — verify Audio Effect Rack and Drum Rack also work via bare name (Drum Rack worked but loaded an Instrument Rack preset by name-collision — see entry above; Audio Effect Rack worked cleanly). (W7-0 session 2026-05-19)
- **`_write_breakpoints_as_steps` for `note_expression` still uses a zero-duration anchor.** W7-0 fixed the clip-scoped paths (mixer / pan / send / device_parameter, plus clip_cc and clip_pitch_bend) to extend the last step to `tail_end=clip.length`. note_expression was deliberately left on the zero-duration anchor path because the handler doesn't currently receive the note's duration. The revert-to-default artifact may be audible / visible on note-expression curves whose last breakpoint isn't at the note's end. Fix: thread note_duration (or note_end_beats) through `write_envelope_handler`'s note_expression branch and pass `tail_end=note_end_beats` to `_write_breakpoints_as_steps`. Sized: ~10 LoC + 1 unit test. (W7-0 cumulative-Critic warning 4, 2026-05-19)
- **`_TRANSACTION_DEPTH` module-level state may leak under thread/async patterns.** W7-A's SAVEPOINT-based reentrant `transaction()` keeps depth in a module-level `dict[int, int]` keyed by `id(conn)`. Single-threaded today (per project preferences "Sync throughout. SQLite WAL + timeout=10.0. No async planned"), but if the project ever adopts thread/async patterns the depth counter would interleave. Defensive options: (a) use a `WeakKeyDictionary` keyed by the connection object directly; (b) attach the counter to the connection via a wrapper; (c) explicit `threading.local` if multi-threaded use ever lands. Sized as ~5 LoC + 1 thread-safety test. (W7 cumulative-Critic note 3, 2026-05-19)
- ~~**Populate or strike S-2 through S-6 smoke placeholders in `tests/integration/test_live_smoke.md`.**~~ **RESOLVED (W7-0 session 2026-05-19)** — S-2 and S-4 struck as coverage-by-proxy (S-2 envelope-pull covered by S-1 read verification + `tests/unit/test_pull.py` diff math; S-4 W6-K subsumed by S-1/3/5/6 surface coverage). S-3 / S-5 / S-6 populated and PASSED. New real-Live findings captured separately below. (W7 cumulative-Critic note 4, 2026-05-19)
- **HIGH PRIORITY — Provenance log: capture prompts, round-trip cycles, and "really anything" via the existing `requests` + `events` tables.** The `events` audit log + `requests` higher-level intent table already exist (every mutator emits an event tied to a request — project invariant per architecture memory). The bones are there, but a lot of useful provenance is being thrown away today:
  1. **Prompts.** When the user (or Claude) drives composition, `requests.intent` ends up holding terse labels like `"create song"` or `"set track volume"`. The actual LLM-facing prompt text — *"build a verse that feels like weight getting worse"* — is not captured. Future sessions can't reconstruct *why* a song looks the way it does.
  2. **Round-trip cycles.** Push/pull operations fan out into many per-mutator `actor='sync'` events. There's no top-level row marking "push cycle started → 47 plan calls → 3 warns → 0 failures → finished at T+8.2s." The cycle as a discrete operation is invisible.
  3. **Compose sessions.** No record of "this Claude session (model X, git SHA Y, branch Z) touched the song between T0 and T1." Cross-session forensics requires manual scanning of `events.ts`.
  4. **Snapshots / captures.** `capture.replay_capture` writes events with `actor='sync'`, but there's no header row tying the snapshot timestamp + Live-set identity to the event burst.

  **Shape: extend `requests`, don't add a parallel table.** Today `requests` has `(id, ts, actor, intent, payload_json, song_id)`. Proposed additions:
  ```
  kind         TEXT NOT NULL DEFAULT 'mutate'  -- 'compose' | 'push' | 'pull' | 'capture' | 'analyze' | 'mutate'
  prompt_text  TEXT                            -- full LLM-facing prompt when applicable
  duration_ms  INTEGER                         -- wall-clock duration for cycle-shaped requests
  outcome      TEXT                            -- 'ok' | 'partial' | 'failed' (NULL during open cycle)
  parent_id    TEXT REFERENCES requests(id)    -- nest compose-session under cycle (or vice versa)
  metadata_json TEXT                           -- {model, git_sha, branch, session_id, hostname, ...}
  ```
  The existing `payload_json` already accepts arbitrary JSON; `prompt_text` is split out so it's first-class queryable. `kind` lets queries answer "show me every push cycle" vs "show me every prompt."

  **Mutator surface — entry points (DON'T inline in every callsite):**
  - `M.open_request(conn, *, kind, intent, prompt_text=None, song_id=None, parent_id=None, ...) -> request_id` — opens a request, returns id; all subsequent mutator events tie to it. Already exists in spirit; needs the new kwargs.
  - `M.close_request(conn, *, request_id, outcome='ok', duration_ms=None)` — marks a cycle done.
  - Context-manager wrapper: `with M.request(conn, kind='push', intent='falling-walking → live', prompt_text=...) as rid:` — auto-closes on exit, records duration. Reduces ceremony.

  **Wiring touchpoints:**
  - **Push/pull skill drivers** open a `kind='push'` / `kind='pull'` request before fan-out, close it after `apply_*_results`. Sync events automatically nest underneath via `request_id`.
  - **Capture** opens `kind='capture'`, attaches Live-set path + timestamp to `metadata_json`.
  - **Claude Code compose sessions** open `kind='compose'` at session start with `prompt_text` = the user's initial message, `metadata_json` = {model, git_sha, branch}. Sub-actions nest via `parent_id`. This is the entry point the user can write in their session-briefing hook.
  - **The MCP server's per-tool dispatcher** could auto-open a `kind='mutate'` request per top-level tool call if no parent exists, capturing the tool's args as `prompt_text` (degraded but always-present provenance).

  **Query surface:**
  - `Q.get_requests_for_song(conn, song_id, *, kind=None, since=None)` — timeline.
  - `Q.get_events_for_request(conn, request_id)` — drill-down on a cycle.
  - `Q.get_compose_history(conn, song_id)` — kind='compose' filtered, ordered, with prompt previews.
  - `Q.find_related_decisions(conn, song_id, *, keywords, scope=None)` — substring/keyword search across `requests.prompt_text` + `requests.metadata_json.decision_rationale` + `annotations.body`. Returns ordered hits with timestamps + (optionally) parent context. Single SQL `LIKE` pass is enough for v1; semantic search (embeddings) is a follow-on if the dataset grows.

  **Read-path UX — this is the payoff and the design driver.** Provenance is *context the LLM uses when collaborating on the song*, not a passive audit. The retrieval surface enables both directions of musical conversation:

  **Defensive — challenge contradictions:**
  > User: *"Accentuate the backbeats in the verse."*
  > Claude (after retrieving prior decisions): *"Actually we brought those down on 2026-05-26 to contrast with the chorus — happy to adjust if you want to revisit that, but are you sure?"*

  **Generative — propose connections the user hasn't drawn yet:**
  > User: *"Let's make the break go dim7."*
  > Claude (after retrieving structural facts + prior decisions): *"That'll work great with the intro that starts dim7 and goes major — let's echo the first few notes."*

  The defensive case prevents regression on deliberate choices. The generative case is the bigger payoff: the LLM has the whole song's structural + intentional context in working memory and **does the creative connection-drawing on its own** once the data is findable. The system's job is to surface broad relevant context cheaply, not to encode musical similarity (per project memory `feedback_prefer_llm_over_deterministic_module`).

  This requires three disciplines, not just schema:
  1. **Decisions are recorded WITH RATIONALE, not just actions.** When the LLM makes a non-trivial compositional choice, the `requests` entry that wraps it MUST capture the *why*, not only the *what*. Convention: `requests.intent` = the user's surface request; `requests.metadata_json.decision_rationale` = the LLM's reasoning that produced the specific values. The mutator events under it carry the mechanical change (velocity drop, envelope edit) — the request carries the meaning. Structural facts that aren't decisions per se (e.g., "the intro is dim7 → major") rate the same treatment via annotations so they ride along the same retrieval surface.

  2. **Before non-trivial work, the LLM retrieves prior context — broadly.** Not just exact-match against the user's prompt; the retrieval should pull in:
     - Decisions about the SAME element ("the break") — defensive case.
     - Decisions/annotations about RELATED CONCEPTS ("dim7" anywhere, harmonic moves, the bridge's chord pattern) — generative case.
     - STRUCTURAL FACTS about adjacent sections — the LLM can draw connections only if it can see them.

     Add a `/decisions [topic]` skill (analogous to `/learnings`) or a pre-task pattern: at the start of any compositional ask, run `find_related_decisions` with both the prompt's noun-phrases AND a broader semantic dragnet (related sections, related concepts the LLM extracts before searching). Surface the hits BEFORE acting; the LLM uses them to either challenge gently (defensive) or propose connections (generative). The LLM owns the creative synthesis — don't bake musical-similarity logic into the retriever.

  3. **The session briefing surfaces decisions + structural facts, not just recent activity.** "Last 5 prompts" is weaker than "last 5 *deliberate decisions with rationale*" combined with "key structural annotations spanning the whole song." For a song with 3 sections, surface the section-scoped annotations on every session start — they're the working-memory shorthand a human collaborator would carry between sessions. Filter on `metadata_json.decision_rationale IS NOT NULL` (or a `kind='decision'` sub-classification) plus include all `annotations` with `kind IN ('intent', 'structure')`.

  This is also where provenance meets annotations: a deliberate compositional decision SHOULD often also produce a permanent annotation (the rationale graduates from request metadata to a song-scoped or section-scoped annotation if it'll guide future work). Provenance captures every decision in real time; annotations are the curated subset worth keeping forever. A small helper `M.promote_decision_to_annotation(conn, request_id)` would automate the common case.

  **Relationship to the song-annotations entry below:** annotations are AUTHOR-FACING intent (the *why* of the song, written deliberately, read forward to drive composition). The provenance log is PROCESS-FACING history (the *how* the song got built — but with the rationale-discipline above, also *why* each decision was made at that moment). Provenance captures every decision in real time as the song evolves; annotations are the curated subset worth keeping forever. They share a retrieval pattern (`find_related_decisions` queries both) and a promotion path (provenance entries with durable rationale can be promoted to annotations).

  **Out of scope for v1:** transcript persistence (just the seed prompt — full conversation transcripts live in Claude Code's own storage and would balloon the DB); multi-user attribution (single-user assumption holds); cross-DB merge of requests (forklift to event-store flip).

  **Sizing:** ~1 chunk for schema + mutator entry points + a few wiring points (push/pull/capture drivers). Session-briefing integration + LLM-side prompt-capture hook is a separate small chunk. MCP-side auto-capture on tool calls is a stretch chunk if it ever lands.
  (Feature request 2026-05-19; user-flagged HIGH PRIORITY — sibling to annotations entry)

- **HIGH PRIORITY — Song annotations: persist composer intent + stylistic notes alongside the song data.** Today the DB stores structure (notes, envelopes, devices, arrangement) but not the *meaning* behind it. The LLM-native workflow needs a place to accumulate the why: "the verse is sad, like weight getting worse; then the chorus it just evaporates," "last chorus goes around once in minor, then once in min7," "this bass line is weaving between drum parts at 95 and 100 BPM," "don't sidechain the bass on the bridge — let it bloom." Updated continuously as composition progresses; read back by every future agent session for context. **Why this is high priority:** annotations ARE the song requirements, captured in-place. Without them, every new session starts cold — Claude can read the structural DB but not the intent, so it can't act on aesthetic decisions the user already made. Bundle the requirements into the song itself, not in a separate brief.

  **Proposed shape — single polymorphic `annotations` table:**
  ```
  annotations (
    id            TEXT PRIMARY KEY,
    song_id       TEXT NOT NULL REFERENCES songs(id) ON DELETE CASCADE,
    track_id      TEXT REFERENCES tracks(id) ON DELETE CASCADE,  -- nullable
    start_bar     REAL,                                           -- nullable
    end_bar       REAL,                                           -- nullable (open-ended OK)
    kind          TEXT NOT NULL,    -- 'intent' | 'stylistic' | 'structure' | 'reference' | 'todo'
    body          TEXT NOT NULL,
    created_at    REAL NOT NULL,
    updated_at    REAL NOT NULL,
    CHECK (end_bar IS NULL OR end_bar > start_bar),
    CHECK (start_bar IS NOT NULL OR end_bar IS NULL)
  )
  ```
  Three scoping levels fall out of column nullability:
  - **Song-scoped** = `track_id` NULL, `start_bar` NULL → applies to the whole song.
  - **Time-scoped** = `track_id` NULL, `start_bar` set, `end_bar` optional → applies to a time range or point in song time.
  - **Track-scoped** = `track_id` set, time optional → applies to a track (optionally over a time range).

  Name conflict caveat: existing `notes` table is MIDI notes. `annotations` disambiguates cleanly; alternatives considered: `commentary`, `prose_notes`.

  **Mutator surface:**
  - `M.add_annotation(conn, *, song_id, track_id=None, start_bar=None, end_bar=None, kind, body, ...)`
  - `M.update_annotation(conn, *, annotation_id, body=None, kind=None, ...)`
  - `M.delete_annotation(conn, *, annotation_id, ...)`
  - Events: `ANNOTATION_ADDED` / `ANNOTATION_UPDATED` / `ANNOTATION_REMOVED`.

  **Query surface:**
  - `Q.get_annotations_for_song(conn, song_id, *, kind=None)` — all
  - `Q.get_annotations_for_track(conn, track_id)` — track-scoped only
  - `Q.get_annotations_at_bar(conn, song_id, bar)` — time-active at bar (start_bar ≤ bar < end_bar, or unbounded end)

  **MCP surface (DB-only domain — no Ableton round-trip; Live doesn't model these):**
  - `ableton_annotation` tool OR action on `ableton_session` — single namespace for `list` / `add` / `update` / `delete` / `get_at_bar`.
  - Resources: a `hallucinote://annotations/<song_id>` resource for cheap full-song reads.
  - Prompts: a `compose_with_intent` prompt template that walks the LLM through reading annotations BEFORE generating.

  **Session-start integration:** the session briefing (or a CLAUDE.md addendum) should surface "song annotations exist; read them before composing." Without this nudge, the LLM may build without context even with the data available.

  **Out of scope for v1 of this feature:** versioning of annotation bodies, multi-user attribution, attachments (only text). Composer attribution rides on the existing `events.actor` field.

  **Sizing:** ~1-2 chunks. Schema + mutators + queries + events (one chunk); MCP surface + skill integration + session-briefing wiring (one chunk).
  (Feature request 2026-05-19; user-flagged HIGH PRIORITY)

- **Envelope discovery on pull — envelopes authored only in Live.** W7-A (2026-05-19) ships `plan_pull_envelopes` in **DB-mirrored mode**: it iterates `envelopes` rows that already exist in the DB and refreshes their breakpoints from Live. This closes the round-trip case (DB-authored envelope → user tweaks in Live → pull → DB updated). It does NOT discover envelopes the user authored *only* in Live without a corresponding DB row — those would require enumerating every linked clip × supported `target_kind`, every linked device parameter, etc., which explodes the read surface (~10s-100s of probes per pull on a real song). A future "envelope discovery" pass could batch-probe likely surfaces (e.g. only on clips/devices/tracks the user mutated recently per `events` log) and fan out judiciously. Not blocking V1 — the round-trip story closes the dominant authoring loop. (W7-A 2026-05-19)
- **Multi-bar tempo / time-signature automation — INVESTIGATION CLOSED, NO MCP-SIDE FIX POSSIBLE (W6-F 2026-05-19).** The bar-1 case is solved — W5-A (2026-05-18) rewrote `plan_push_tempo_map` / `plan_push_time_signature_map` to emit `ableton_session(set_tempo)` / `set_signature` for bar-1 rows directly. The multi-bar half cannot be closed via MCP: **the underlying Live LOM does not expose `create_automation_envelope` from any song-level path.** Across `gluon/AbletonLive12_MIDIRemoteScripts` and 25+ third-party Live MCP/Remote-Script repos (including ClyphX, the most aggressive Live-automation toolkit), `create_automation_envelope` is called EXCLUSIVELY on `Clip` objects. `song.master_track.mixer_device.song_tempo` IS a `DeviceParameter` but has no envelope-creation path; the Mixer chooser path is GUI-only. Time-signature is worse — `signature_numerator` / `signature_denominator` are plain int properties, not `DeviceParameter` objects, and per Ableton's own forum (t=144193) time-signature automation is unsupported in the API. Sources: Cycling74 LOM, gluon/AbletonLive12 `_MxDCore/LomTypes.py`, Live 12 release notes, Ableton manual. **Supported workarounds**: (a) **per-scene tempo/signature** via `ableton_scene` — scenes carry their own tempo/sig and trigger on launch; works for songs structured around scenes; (b) **real-time step-write** via scheduled `song_tempo.value` writes against `current_song_time` — degraded (lost on reload, requires playback). The Hallucinote planner's "warn and skip non-bar-1 rows" behavior is the right shape until Ableton extends the API. The per-scene workaround is a sync-side architectural change (see follow-on entry below). (corrected W4-E entry; W5-A 2026-05-18; investigation closed W6-F 2026-05-19)
- **Per-scene tempo/signature as the supported workaround for the multi-bar gap.** Hallucinote DB stores `tempo_map` and `time_signature_map` keyed by `start_bar`. Live exposes per-scene tempo/sig (each session-view scene can override the global values when launched). A sync-side change could map "bar X starts a new section" → "create a scene with tempo Y at that bar boundary," giving users multi-bar tempo/sig in the supported architecture without depending on the missing LOM envelope API. Scope: schema-level decision on scene-bar binding, planner emit logic in `plan_push_tempo_map` (use scene path when non-bar-1 rows are present + scenes are part of the song's structure), test coverage. Not in Wave 6 — separate workstream once a song actually needs multi-bar tempo (falling-walking doesn't). (W6-F 2026-05-19)
- **W6-K real-Live smoke deferred.** Wave 6 shipped a substantial MCP-side surface (W6-Probe introspect, W6-E capability-probing sidechain, W6-G/H envelope read via sampling, W6-I/J nested rack chain) all validated against fakes. Real-Live empirical confirmation requires `/ableton-mcp-install` + full Live restart + `/mcp` reconnect (the install ceremony is non-trivial — see ableton-mcp-install SKILL.md). When user has time + a real song open: validate (a) read_envelope round-trips on a mixer_volume / device_parameter envelope; (b) get_device_chains structure on a real rack (e.g., Drum Rack with kit samples); (c) load_in_rack + set_parameter_in_rack on an InstrumentGroupDevice; (d) set_sidechain on a Glue Compressor (the new capability-probing path); (e) set_input_routing finds the right RoutingType by display_name. Also bundles W5-F deferred validation: round-trip parity on parameter-dialed native instruments via the W5-D pull path. (W6 close-out 2026-05-19)
- ~~**W6-G/H: real-Live confirmation of `clip.create_automation_envelope` idempotency.**~~ **RESOLVED — empirical answer: NOT idempotent (W7-0 smoke 2026-05-19).** Real Live's `create_automation_envelope(target)` returns None (or raises) when an envelope is already bound to that target — opposite of the W6-G assumption. The S-1 smoke caught this; fix shipped same session: `_find_existing_envelope` now iterates `clip.automation_envelopes` and matches by parameter identity (the fallback the design anticipated). `envelope.parameter` IS accessible on Live 12.4 (contrary to a stale codebase comment that has been corrected). Sibling write-side finding caught + fixed in the same pass: `_write_breakpoints_as_steps` was leaving a zero-duration anchor at the last breakpoint, which let Live's revert-to-default behavior leak through as a visible 0dB / 0-pan point between the last breakpoint and clip end — now extends the last step to `tail_end=clip.length` for clip-scoped envelopes. FakeClip updated to mirror real Live's non-idempotent behavior so the unit suite catches this class of bug going forward. (W6-G/H 2026-05-19, resolved W7-0 2026-05-19)
- **Capture-side: walk `get_device_chains` to populate `device_chains` rows.** The capture layer's `{_note: "Rack — internal chain instruments not captured"}` flag is now closeable post-W6-I via `ableton_device(action='get_device_chains')`. Extending `tools/capture.py` + `compile_snapshot` to recurse one level deep and write `device_chains` rows with `parent_rack_device_id` is the natural follow-on. (W6-I 2026-05-19; supersedes the earlier "Capture extension for nested rack chains" entry which was framed against the gap-blocked state)
- **Recursive nested-nested rack chain support.** W6-I/J ship one-level-deep nested-rack support (chain_index + nested_device_position). Live allows racks-inside-racks-inside-racks; addressing beyond one level requires a path-style API (e.g., `chain_path=[2, 1, 3]`). Not exercised by today's songs; defer until a real song needs it. (W6-I/J 2026-05-19)
- **W4-E real-Live finding: Live 12.4 envelopes are step-only — PLAN-TIME WARN SHIPPED (W5-E 2026-05-19); INVESTIGATION CLOSED (W6-C 2026-05-19).** `plan_push_envelopes` emits a per-envelope `plan.warn` when any breakpoint has `curve_kind in {'linear', 'fast', 'slow'}`, surfacing round-trip lossiness at plan time. Only `'hold'` curves round-trip losslessly. **Investigation outcome (W6-C)**: no segment-curve API exists in Live 10–12. `Live.Clip.AutomationEnvelope` exposes exactly two public methods (`insert_step(time, duration, value)`, `value_at_time(time)`) across every Live version. Ableton's own Push remote script (`pushbase/automation_component.py:164` in the Live 12 branch) calls only `insert_step`. Live 12 release notes mention no envelope API additions. Sources: Structure-Void's auto-generated LOM XML (Live 10.1.19, generated by Ableton's `API_MakeDoc`), https://github.com/gluon/AbletonLive12_MIDIRemoteScripts (zero matches for `insert_smooth`/`add_segment`/`insert_segment`/`insert_linear`), https://www.ableton.com/en/release-notes/live-12/. **Residual**: (a) push SKILL.md prose call-out (W6-K close-out); (b) workaround if curve fidelity is required — emit a dense sequence of stepped points approximating the desired curve (XML bloat + post-load editability cost). The structural close is on Ableton's roadmap, not ours. (real-Live smoke, feat/wave-4 W4-E, 2026-05-18; warn shipped W5-E 2026-05-19; investigation closed W6-C 2026-05-19)
- **W4-E real-Live finding: native Live instruments with `preset_uri=None` round-trip as blanks — STRUCTURALLY RESOLVED (W5-D 2026-05-19).** `plan_pull_device_parameters` + `_apply_device_parameters_for_device` write `device_parameters` rows from `ableton_device(get_parameters, detail='full')` probes. Push's `plan_push_devices` already emits `set_parameter` per row (push.py:958-965); the round-trip closes once a song has been through a `device-parameters` pull. **Remaining**: W6-K real-Live smoke on user-dialed patches confirms actual sound-parity round-trip (formerly W5-F; folded into Wave 6 close-out per the W6 plan). (W5-D 2026-05-19; W5-F → W6-K 2026-05-19)
- **W4-E real-Live finding: Live 12.4 hides the mixer column on tracks with empty device chains.** Empirical (real Live 12.4, falling-walking smoke 2026-05-18): MIDI tracks created via `ableton_track(action='create')` with no devices loaded show NO volume/pan/sends/master faders in either Session or Arrangement view. Loading any device into the chain causes the mixer column to appear. The mixer state IS settable via MCP regardless (the volumes/pans/sends written in the smoke ARE there) — Live just hides the UI surfaces until the chain is populated. Add to push skill prose: warn users that empty-chain tracks have invisible mixer until devices land in phase 7. Not a Hallucinote bug; a Live 12.4 UX quirk worth surfacing. (real-Live smoke, feat/wave-4 W4-E, 2026-05-18)
- **`sidechain_trigger`: principled section-clamp for the first attack window.** W4-B exposed that `sidechain_trigger`'s first `attack_start = hit - attack_beats` can fall before the section boundary, so the envelope's beat range spills outside the chorus's session-clip placement and the planner skips it. falling-walking's build.py works around this by shifting EVERY chorus kick by `attack_beats` (~5ms late at 132bpm). Principled fix: add an `envelope_start_beats` parameter to `sidechain_trigger` (or have it floor `attack_start` to a configurable section start), so the generator clips the pre-attack window without shifting the hits themselves. Sized as: ~10 LoC in `generators/envelopes.py` + 2 tests + revert falling-walking's build-site shift. (critic feat/wave-4 W4-B W1, 2026-05-18)
- ~~**W4-B planner: multiple distinct covering clips on the same track.**~~ **RESOLVED (W7-C 2026-05-19)** — `_resolve_envelope_session_clip` now collects all distinct covering clips, `_warn_multiple_covering_clips` emits a `plan.warn` naming the others when more than one cover. Test: `test_warns_when_multiple_distinct_clips_cover_envelope_range`.
- ~~**W4-B planner: trimmed placements where end_bar - start_bar < source.length_beats.**~~ **RESOLVED (W7-C 2026-05-19)** — `_CoveringPlacement.trimmed_end_beats` is populated when the placement's `end_bar` < source length; `_warn_trimmed_placement` warns when `env_max` exceeds the trimmed extent. Test: `test_warns_when_placement_trimmed_shorter_than_envelope`.
- **W4-B planner: rule-of-three consolidation across the three `_emit_*_envelope` helpers.** `_emit_mixer_envelope` / `_emit_send_envelope` / `_emit_device_parameter_envelope` all follow the same shape: compute envelope range → resolve placement → check clip link → translate breakpoints → emit + extra-placement warn. Differences are local (target_kind, parent_arg, args dict, error text). Worth consolidating into a single `_resolve_and_translate_to_session_clip` helper that returns a `(clip_at, local_bps, placement)` tuple or `None`. Sized as: ~50-line refactor of the three emitters, no behavior change. (critic feat/wave-4 W4-B N3, 2026-05-18)
- **`_FINGERPRINT_PATHS` binary-safety invariant lives only in the docstring.** W4-F's `_hash_file` normalizes `\r\n → \n` before hashing on the assumption that every fingerprinted entry is Python source (true today). A future contributor adding a non-Python entry (a JSON manifest, an `.so`, a static `.als` skeleton) could silently corrupt the hash because the byte replacement isn't safe for arbitrary binaries. Options: (a) skip normalization for entries that fail a NUL-byte sniff, (b) import-time validator that asserts each resolved fingerprint file ends in `.py`. Tiny — one helper + one test. (critic feat/wave-4 W4-F N1, 2026-05-18)
- **duplicate_to_arrangement: spurious-clip detection is start-time-only.** Chunk W2-H detects the B-24 side effect by comparing arrangement_clips' start_times before vs after the call. Edge case: if a pre-existing clip already sits exactly at `dest_beats + source.length`, its start_time is already in the before-set, and the new spurious clip slips past detection (because both have the same start_time key). A more robust scheme would use object identity (`id(c)`) for the diff — though Live's wrapper recreation (B-1) makes that fragile too. Unlikely in real songs; backlog candidate. (critic W2 N2, Chunk W2-H 2026-05-18)
- **Confirm Phaser/Flanger and Eq3/FilterEQ3 round-trip class_name preservation in real Live.** Chunk W2-B's `device_names` table maps both `Phaser` and `Flanger` to display `Phaser-Flanger` (similarly `Eq3` / `FilterEQ3` → `EQ Three`, `AutoPan` → `Auto Pan-Tremolo`). Live merged these device families under one browser node in 12.x. Load works for either source class_name, but when the device is then captured (`device.list`), the reported `class_name` may be the merged form rather than the original — breaking deterministic re-push for songs that captured a `Phaser` (which would re-capture as `Flanger` after push or vice versa). During the Wave-2 real-Live validation pass, load via `kind='Flanger'`, then re-capture and verify `class_name`. If it isn't preserved, the planner needs to emit `preset_uri` for these merged-display devices instead of relying on class-name resolution. Sized as: real-Live verification + (if needed) ~50 LoC planner change. (critic W2 N1, Chunk W2-B 2026-05-18)
- **Enrich the flat-wire JSONSchema with `enum` / `minimum` / `maximum` / `description` from `ParamSpec`.** Chunk W2-1 synthesized per-tool wrappers whose signature exposes every action's params as keyword-only Optionals — FastMCP's JSONSchema then surfaces them at the top level (matches every doc/example). But the schema today only propagates Python type + Optional; `ParamSpec` already carries `enum` (e.g., `target_kind` across the 7 envelope families), `minimum` / `maximum` (e.g., `cc_number` 0-127, `bpm` 20-999), and `description`. The dispatcher still enforces these at runtime via teaching errors so correctness is intact, but agents could prune impossible calls earlier with a richer schema. Wire it via `typing.Annotated[type, Field(...)]` or by manually editing the generated schema post-registration. Sized as a small refactor in `hallucinote_mcp/src/hallucinote_mcp/server.py::_register_tool` + a wire-schema test confirming enum/min/max round-trip. (critic W2, Chunk W2-1 2026-05-18)
- **FastMCP private-API access in `test_server.py`.** Three new tests in Chunk W2-1 (`test_tool_call_via_fastmcp_accepts_flat_kwargs`, `test_tool_call_via_fastmcp_drops_unsupplied_optional_kwargs`, `test_help_action_works_via_fastmcp_with_no_kwargs`) reach into `mcp._tool_manager._tools[name]` directly to fetch a `Tool` for `.run()`. The existing `registered_tool_names` helper tries multiple attribute names for FastMCP version-drift resilience; a symmetric `get_registered_tool(mcp, name)` helper would centralize the version-coupling and keep these tests robust. (critic W2 N1, Chunk W2-1 2026-05-18)
- ~~**Return-track coverage for clip-scoped automation paths.**~~ **RESOLVED (W7-C 2026-05-19)** — 4 new tests in `hallucinote_mcp/tests/unit/test_actions_automation.py` pin the `return_index` branch of `_require_parent` for `mixer_volume` / `mixer_pan` / `send_level` (return-to-return) / `device_parameter` clip-scoped envelopes. `send_level` uses a teaching-error-OR-success contract because `return_index` addressing is ambiguous between source and target.
- **Clear + note_expression: omit-required-args path untested.** Today, `ableton_automation(action='clear', target_kind='note_expression')` raises the gap-citing `NotImplementedError` regardless of whether note_pitch / note_start_beats / axis were supplied (the gap check fires before parameter validation). Asymmetric with `write_envelope` which validates first. Either add a docstring note acknowledging the precedence or add a one-line test pinning the behavior. (critic, Chunk D 2026-05-18)
- **Backlog: return-side device_parameter envelopes need a return-track session-clip model.** W4-B routes track-side mixer/pan/send/device_parameter envelopes through session clips on the parent track. Return tracks have arrangement-side mixer state but the DB has no session-clip model for returns (`clips.track_id` references `tracks(id)` only). Live 12.4 only accepts device_parameter envelopes on session clips, so return-side envelopes get a warn+skip today. Closing the gap is non-trivial: add a return-clip schema branch + mutators + push/pull routing. Not blocking V1 — falling-walking has no return-side envelopes. (W4-B 2026-05-18)
- **W4-C suffix-only invariant lacks structural enforcement.** W4-C established that `returns.name` stores SUFFIX-only (Live re-adds the slot prefix on push). The invariant is enforced only by writer discipline (`capture.py` strip + `pull.py` strip); a future song `build.py` calling `M.create_return(conn, name="A-Reverb", ...)` would silently re-poison the DB. Options: (a) add `CHECK (name NOT GLOB '[A-Z]-*')` to the `returns` schema, (b) push the strip into `M.create_return` / `M.update_return` (the only sanctioned write paths) so all callers benefit automatically. Option (b) better fits the project's mutator discipline. Sized as: small mutator change + schema CHECK + 1-2 tests. (critic feat/wave-4 W4-C N1, 2026-05-18)
- **W4-C send warnings use mixed identity convention.** `_apply_track_sends_for_track` warns with `raw_name` (Live's prefixed form) for the "no matching return" case but with `return_name` (stripped DB form) for the "rejected by mutator" case. Both are defensible — the first identifies what Live reported (helpful for "where is this come from"), the second identifies the DB row (consistent with the detail messages) — but a debugger seeing both warnings for the same send sees two different identifiers for the same return. Pick one convention and document it (or surface BOTH forms in every warning: `"Live='A-Reverb' DB='Reverb'"`). Sized as: ~5 LoC + doc comment. (critic feat/wave-4 W4-C N2, 2026-05-18)
- **Wave plan headers missing top-level `Requirements Confidence` field.** Each M+1 chunk has an inline Confidence check, but the wave-level header in `build-plan.md` lacks a `Requirements Confidence: High|Medium|Low` declaration. Methodology cleanup — apply to the next wave header (M+2 or whatever follows) rather than retrofitting M+1's plan. (critic, M+1 final 2026-05-17)
- **Arrangement-VIEW state pull (loop region, follow mode, view zoom).** Distinct from arrangement-clip-placement pull (which M+1-3b ships). `ableton_arrangement(action='info')` exposes the view state but there is no DB home for loop region or view zoom today, and the tempo/signature it returns are better diffed against `tempo_map`/`time_signature_map` via dedicated probes that match the schema's map shape. Decide whether to add DB columns for view state (probably on a `ableton_sessions` row, as it's session-bound view state, not authored song data) — or leave view state non-round-tripped per Vision's "DB is the score, not the rehearsal-room state." Filed for explicit decision, not silent drop. (reflection, M+1-3 re-plan 2026-05-17)
- **MCP gap: `hallucinote-mcp` envelope read surface.** Wave M-4 unified envelope WRITES via `ableton_automation(action='write_envelope', target_kind=…)` across all seven target families (`clip_cc`, `clip_pitch_bend`, `note_expression`, `device_parameter`, `mixer_volume`, `mixer_pan`, `send_level`). No symmetric READ exists — no `list_envelopes`, no `get_envelope`. Live exposes envelopes; this is purely a `hallucinote-mcp` server gap (not a Live API limitation). Required for envelope pull. Per-target-family read actions probably (mirror the write `target_kind` enum). (V1 close-out 2026-05-17; promoted from inline `docs/mcp-requirements.md` "Capture-side read" §)
- **MCP gap: nested rack chain probe + push.** Live supports rack devices (`DrumGroupDevice`, `InstrumentGroupDevice`, `AudioEffectGroupDevice`) that own nested device chains. Hallucinote's schema *models* nested chains (via `device_chains.parent_rack_device_id`) but capture/replay/push stay flat — `hallucinote-mcp` exposes no probe inside racks. Required: `get_device_chains(track_index, device_index)` → per-chain device list; `load_device_in_rack(track_index, parent_device_index, chain_index, position, kind, preset_uri?)`; `set_rack_device_parameter(track_index, parent_device_index, chain_index, device_index, parameter_name, value)`. Sizable ask but drum kits + modular-style instruments don't fully round-trip without it. Affects: capture, pull, push. (V1 close-out 2026-05-17; merges + sharpens the prior "Capture extension for nested rack chains" entry which was incorrectly framed against gap #17b)
- **Live API residual on gap #4: true surgical Ableton-side note writes.** V1 close-out Chunk D shipped READ-with-stable-IDs + WRITE-whole-clip (sufficient for compose/produce: `ableton_note(action='list')` + `clip-notes` pull domain + `ableton_clip(action='replace_notes')`). The residual gap is per-note Ableton writes that preserve playback continuity (Live retriggers a clip on `set_notes` during playback) — would land via `apply_note_modifications` / `add_new_notes` / `remove_notes_by_id`. Out of V1 scope per `scope.never` (no live-performance use case). Re-open if/when performance use cases enter scope. (V1 close-out 2026-05-17; updated after Chunk D shipped)

- **Hallucinote-side quantize / swing / groove module.** During Wave M-3, the user reaffirmed the DB-as-source-of-truth principle for note timing: quantize, swing, and groove templates are pure-math transforms that should live in Hallucinote (Python/SQL), not call Live's `clip.quantize()` or use Live's Groove Pool. The MCP surface therefore exposes NO `quantize` / `apply_groove` / `extract_groove` actions; Hallucinote computes the result and pushes pre-grooved notes via `ableton_clip(action='replace_notes', ...)`. Scope: (a) `src/hallucinote/timing/quantize.py` — pure functions taking `(notes, grid, amount, swing) → notes`; (b) `grooves` table for storable timing templates portable across songs; (c) `apply_groove(notes, groove_template) → notes` and `extract_groove(notes) → groove_template`; (d) generator-layer wiring so `generate_*` outputs can optionally pass through quantize/groove before persistence. Sized as a single chunk after Wave M completes (or sooner if a song needs it). Test discipline: example-based fixtures for known input→output; consider Hypothesis property tests for the round-trip invariant (extract-then-apply approximately recovers the template). (reflection, Wave M-3 user pivot 2026-05-17)
- **Audio render + analysis (`ableton_render` + `ableton_analysis`).** User-flagged as "sooner than later, but not yet." Sketched in `docs/mcp-tool-design.md` §17 (Tier 3). Async-shaped (render returns job handle; analysis takes file path). Wave M+1 candidate after `hallucinote-mcp` v1.0 ships. Open question: analysis lives in `hallucinote-mcp` or a separate `audio-analysis-mcp`? (reflection, session 2026-05-16)
- **Wider legacy-tool prose rewrite in `songs/falling-walking/falling-walking.md`.** J-3 caught the two explicit `mcp__AbletonMCP__*` references (line 226 sibling-repo path + line 280 `list_external_plugins`) but the "Tool quirks to know" block at lines 281-284 (`get_cue_points`, `add_notes_to_clip`, `create_clip`, `duplicate_clip_to_arrangement`) and line 227 ("AbletonMCP control surface is installed at the Music path") still use the legacy fork's mental model. These need an agent-consumption-focused rewrite against the v1.0 `mcp__hallucinote-mcp__*` action-dispatch surface. Sized as a small docs chunk; defer until the song agent prose actively misleads someone. (janitor, J-3)
- **Module-size watch: `src/hallucinote/db/mutations.py` (2117 lines), `sync/push.py` (1333 lines), `sync/pull.py` (1089 lines).** Cohesion is high (per-domain helpers cluster naturally), but at ~4.5kLOC across three files the per-domain split (e.g. `mutations/clips.py`, `mutations/devices.py`, `mutations/automation.py`) becomes attractive once the suite exceeds ~30s or a new contributor onboards. No action today; revisit when either trigger fires. (janitor, J-3)
- **Name collision: `tools/capture.py` (CLI wrapper, ~50 lines) vs `src/hallucinote/capture.py` (412-line module).** Both files share a leaf name; the CLI wrapper just invokes `python -m hallucinote.capture` style. A fresh reader can be momentarily confused about which is "the capture module." Either rename the CLI wrapper (e.g. `tools/capture-cli.py`) or add a one-line header comment to both pointing at the other. Non-blocking. (janitor, J-3)
- **Migration tool `tools/migrate_arrangement_clip.py` has no automated tests.** Manual round-trip verified end-to-end on falling-walking's real DB (32 placements + 32 events + 3 indexes rewritten; second run no-ops). A small synthetic-fixture test would lock the contract — particularly the JSON1 payload-key rewrite and the idempotency guard against both-tables-present. Sized as a single test file under `tests/unit/tools/`. (PR review #19 + #24, 2026-05-17)
- **Two raw `conn.execute("SELECT ...")` reads in `sync/push.py:996, 1053`.** Not a discipline violation (reads, not writes) but factoring into `queries.py` would localize SQL exposure consistently with the rest of the project. (PR review #24, 2026-05-17)
- ~~**Note pull: no full push-mutate-pull round-trip test.**~~ **RESOLVED (W7-C 2026-05-19)** — `test_clip_notes_end_to_end_round_trip` in `tests/unit/sync/test_pull.py` exercises convergent no-op → drift apply (update + delete + insert) → re-apply idempotent.
- **`_apply_session_clips_for_track` cascades `delete_clip` → `arrangement_clips` silently.** Design-consistent with the project's cascade discipline, but the cross-domain side effect is invisible in `out.details` / no per-row events for the cascaded placements. Worth a follow-up to count + log cascaded arrangement-clip placements when a session-clip delete fires during pull. (PR review #22, 2026-05-17)
- **`_apply_session_clips_for_track` silently tolerates missing `length` / `name` on populated entries** (via `_floats_differ(None, X) → False`), unlike `_apply_arrangement_clips_for_track` which warns explicitly on missing fields. Asymmetry, not a correctness bug. Tighten for parity. (PR review #22, 2026-05-17)
- **`docs/mcp-requirements.md` heading prose under gap #4 still uses pre-Chunk-D framing.** The status summary table and the line-54 backlog rollup were both flipped to "partial"; the deeper Priority-1 narrative still describes note-level addressing as fully blocked. Update the prose to match the partial-shipped state. (PR review #23, 2026-05-17)
- Capture extension for nested rack chains. The chunk-4 snapshot has racks (`InstrumentGroupDevice`, `DrumGroupDevice`) flagged with `_note: "Rack — internal chain instruments not captured"`. Gated on the broader "MCP gap: nested rack chain probe + push" entry above — once `get_device_chains` lands, extend `tools/capture.py` + `compile_snapshot` to recurse into rack chains and populate `device_chains` with `parent_rack_device_id`. (builder, chunk-4 setup; updated 2026-05-17 — the prior "gap #17b" framing was incorrect)
- Master-strip device chains. `device_chains.parent_track_id` accepts the `kind='master'` row, but `plan_push_devices` skips `kind='master'` today. Master devices (master limiter, master EQ) are common in real mixes — needs a master-specific push path (no track_index, reaches the master strip directly) and an MCP gap entry. falling-walking has no master devices today, so this is non-blocking. (builder, chunk 4a; updated 2026-05-17 after the `'return'` kind reservation was dropped)
- Audio clips: clip kind discriminator, file references, warp metadata, warp markers. Deferred from V1. (migrated)
- **Session-view audio clip placement via browser-load workaround (W6-D 2026-05-19 investigation).** Live 10–12 has no `ClipSlot.create_audio_clip` or equivalent — confirmed via LOM XML (Structure-Void Live 11.0), M4L LomTypes whitelist, Push remote scripts, and Live 12 release notes. The only path to put audio in a session slot is async browser-load: set `song.view.highlighted_clip_slot = target_slot`, then `application.browser.load_item(audio_browser_item)`. Caveats: async (no completion callback), audio must be addressable as a BrowserItem (Library/User/Places — not an arbitrary filesystem path), browser-indexing dependent. Could expose as `ableton_clip(action='load_audio_to_session', track_index, clip_index, browser_uri)` mirroring `ableton_device(action='load')`'s browser-driven pattern (W6-D investigation; backlog 2026-05-19)
- Track routing: sidechain, parallel busses, input/output routing config. Schema + sync work. (migrated)
- Group tracks (`tracks.parent_track_id` + Live group semantics). (migrated)
- Pull-side sync follow-on (status as of W5-D 2026-05-19): **Shipped:** mix-state (W3-1), cue points + score globals (W3-2), device chain structure (W3-3 / M+1-2), arrangement-clip placements (W3-4 / M+1-3b), session-view clip slots (V1 close-out Chunk C), note pull via stable-ID read + whole-clip write (V1 close-out Chunk D — gap #4 partial), device-parameter values (W5-D). **Deferred (backlog entries above):** envelope pull (gated on `hallucinote-mcp` envelope read surface), nested rack chain pull (gated on `hallucinote-mcp` `get_device_chains`). (migrated; rewritten V1 close-out 2026-05-17; W5-D update 2026-05-19)
- Event replay function (`replay(events) → state`) and merge tooling. Required for cross-DB event-stream merges between collaborators; only useful after the event-store flip. (migrated)
- Post-hoc event annotation (`event_annotations` table) for narrative on-the-fly addition to history. (migrated)
- Real-time concurrent editing / cloud DB migration. Out of foreseeable scope; the mutator-discipline + storage abstraction already keeps the door open. (migrated)
- Song tests (on-demand layer): DB consistency + mix hygiene + audio/spectral. Land per-chunk as appetite allows; not gating any chunk's completion. (migrated)

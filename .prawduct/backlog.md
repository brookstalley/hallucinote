# Backlog — Hallucinote

<!-- Work discovered during sessions but out of current scope.
     Add items at the top. Each is a bullet with source marker:
     (builder), (critic), (reflection), or (migrated).
     Review with /janitor or when planning new work. -->

- **Envelope discovery on pull — envelopes authored only in Live.** W7-A (2026-05-19) ships `plan_pull_envelopes` in **DB-mirrored mode**: it iterates `envelopes` rows that already exist in the DB and refreshes their breakpoints from Live. This closes the round-trip case (DB-authored envelope → user tweaks in Live → pull → DB updated). It does NOT discover envelopes the user authored *only* in Live without a corresponding DB row — those would require enumerating every linked clip × supported `target_kind`, every linked device parameter, etc., which explodes the read surface (~10s-100s of probes per pull on a real song). A future "envelope discovery" pass could batch-probe likely surfaces (e.g. only on clips/devices/tracks the user mutated recently per `events` log) and fan out judiciously. Not blocking V1 — the round-trip story closes the dominant authoring loop. (W7-A 2026-05-19)
- **Multi-bar tempo / time-signature automation — INVESTIGATION CLOSED, NO MCP-SIDE FIX POSSIBLE (W6-F 2026-05-19).** The bar-1 case is solved — W5-A (2026-05-18) rewrote `plan_push_tempo_map` / `plan_push_time_signature_map` to emit `ableton_session(set_tempo)` / `set_signature` for bar-1 rows directly. The multi-bar half cannot be closed via MCP: **the underlying Live LOM does not expose `create_automation_envelope` from any song-level path.** Across `gluon/AbletonLive12_MIDIRemoteScripts` and 25+ third-party Live MCP/Remote-Script repos (including ClyphX, the most aggressive Live-automation toolkit), `create_automation_envelope` is called EXCLUSIVELY on `Clip` objects. `song.master_track.mixer_device.song_tempo` IS a `DeviceParameter` but has no envelope-creation path; the Mixer chooser path is GUI-only. Time-signature is worse — `signature_numerator` / `signature_denominator` are plain int properties, not `DeviceParameter` objects, and per Ableton's own forum (t=144193) time-signature automation is unsupported in the API. Sources: Cycling74 LOM, gluon/AbletonLive12 `_MxDCore/LomTypes.py`, Live 12 release notes, Ableton manual. **Supported workarounds**: (a) **per-scene tempo/signature** via `ableton_scene` — scenes carry their own tempo/sig and trigger on launch; works for songs structured around scenes; (b) **real-time step-write** via scheduled `song_tempo.value` writes against `current_song_time` — degraded (lost on reload, requires playback). The Hallucinote planner's "warn and skip non-bar-1 rows" behavior is the right shape until Ableton extends the API. The per-scene workaround is a sync-side architectural change (see follow-on entry below). (corrected W4-E entry; W5-A 2026-05-18; investigation closed W6-F 2026-05-19)
- **Per-scene tempo/signature as the supported workaround for the multi-bar gap.** Hallucinote DB stores `tempo_map` and `time_signature_map` keyed by `start_bar`. Live exposes per-scene tempo/sig (each session-view scene can override the global values when launched). A sync-side change could map "bar X starts a new section" → "create a scene with tempo Y at that bar boundary," giving users multi-bar tempo/sig in the supported architecture without depending on the missing LOM envelope API. Scope: schema-level decision on scene-bar binding, planner emit logic in `plan_push_tempo_map` (use scene path when non-bar-1 rows are present + scenes are part of the song's structure), test coverage. Not in Wave 6 — separate workstream once a song actually needs multi-bar tempo (falling-walking doesn't). (W6-F 2026-05-19)
- **W6-K real-Live smoke deferred.** Wave 6 shipped a substantial MCP-side surface (W6-Probe introspect, W6-E capability-probing sidechain, W6-G/H envelope read via sampling, W6-I/J nested rack chain) all validated against fakes. Real-Live empirical confirmation requires `/ableton-install-mcp` + full Live restart + `/mcp` reconnect (the install ceremony is non-trivial — see ableton-install-mcp SKILL.md). When user has time + a real song open: validate (a) read_envelope round-trips on a mixer_volume / device_parameter envelope; (b) get_device_chains structure on a real rack (e.g., Drum Rack with kit samples); (c) load_in_rack + set_parameter_in_rack on an InstrumentGroupDevice; (d) set_sidechain on a Glue Compressor (the new capability-probing path); (e) set_input_routing finds the right RoutingType by display_name. Also bundles W5-F deferred validation: round-trip parity on parameter-dialed native instruments via the W5-D pull path. (W6 close-out 2026-05-19)
- **W6-G/H: real-Live confirmation of `clip.create_automation_envelope` idempotency.** The FakeClip's `create_automation_envelope` is implemented as idempotent (returns existing envelope if present); the W6-G/H read path depends on this behavior. Confirm against real Live during the W6-K smoke — if Live's actual `create_automation_envelope` is NOT idempotent (e.g., always returns a fresh envelope, wiping any existing one), the read path's "create-or-return" probe would silently destroy the very envelope it's trying to read. Mitigation in that case: switch to iterating `clip.automation_envelopes` and matching by parameter identity (already noted as a fallback in W6-G's design). (W6-G/H 2026-05-19)
- **Capture-side: walk `get_device_chains` to populate `device_chains` rows.** The capture layer's `{_note: "Rack — internal chain instruments not captured"}` flag is now closeable post-W6-I via `ableton_device(action='get_device_chains')`. Extending `tools/capture.py` + `compile_snapshot` to recurse one level deep and write `device_chains` rows with `parent_rack_device_id` is the natural follow-on. (W6-I 2026-05-19; supersedes the earlier "Capture extension for nested rack chains" entry which was framed against the gap-blocked state)
- **Recursive nested-nested rack chain support.** W6-I/J ship one-level-deep nested-rack support (chain_index + nested_device_position). Live allows racks-inside-racks-inside-racks; addressing beyond one level requires a path-style API (e.g., `chain_path=[2, 1, 3]`). Not exercised by today's songs; defer until a real song needs it. (W6-I/J 2026-05-19)
- **W4-E real-Live finding: Live 12.4 envelopes are step-only — PLAN-TIME WARN SHIPPED (W5-E 2026-05-19); INVESTIGATION CLOSED (W6-C 2026-05-19).** `plan_push_envelopes` emits a per-envelope `plan.warn` when any breakpoint has `curve_kind in {'linear', 'fast', 'slow'}`, surfacing round-trip lossiness at plan time. Only `'hold'` curves round-trip losslessly. **Investigation outcome (W6-C)**: no segment-curve API exists in Live 10–12. `Live.Clip.AutomationEnvelope` exposes exactly two public methods (`insert_step(time, duration, value)`, `value_at_time(time)`) across every Live version. Ableton's own Push remote script (`pushbase/automation_component.py:164` in the Live 12 branch) calls only `insert_step`. Live 12 release notes mention no envelope API additions. Sources: Structure-Void's auto-generated LOM XML (Live 10.1.19, generated by Ableton's `API_MakeDoc`), https://github.com/gluon/AbletonLive12_MIDIRemoteScripts (zero matches for `insert_smooth`/`add_segment`/`insert_segment`/`insert_linear`), https://www.ableton.com/en/release-notes/live-12/. **Residual**: (a) push SKILL.md prose call-out (W6-K close-out); (b) workaround if curve fidelity is required — emit a dense sequence of stepped points approximating the desired curve (XML bloat + post-load editability cost). The structural close is on Ableton's roadmap, not ours. (real-Live smoke, feat/wave-4 W4-E, 2026-05-18; warn shipped W5-E 2026-05-19; investigation closed W6-C 2026-05-19)
- **W4-E real-Live finding: native Live instruments with `preset_uri=None` round-trip as blanks — STRUCTURALLY RESOLVED (W5-D 2026-05-19).** `plan_pull_device_parameters` + `_apply_device_parameters_for_device` write `device_parameters` rows from `ableton_device(get_parameters, detail='full')` probes. Push's `plan_push_devices` already emits `set_parameter` per row (push.py:958-965); the round-trip closes once a song has been through a `device-parameters` pull. **Remaining**: W6-K real-Live smoke on user-dialed patches confirms actual sound-parity round-trip (formerly W5-F; folded into Wave 6 close-out per the W6 plan). (W5-D 2026-05-19; W5-F → W6-K 2026-05-19)
- **W4-E real-Live finding: Live 12.4 hides the mixer column on tracks with empty device chains.** Empirical (real Live 12.4, falling-walking smoke 2026-05-18): MIDI tracks created via `ableton_track(action='create')` with no devices loaded show NO volume/pan/sends/master faders in either Session or Arrangement view. Loading any device into the chain causes the mixer column to appear. The mixer state IS settable via MCP regardless (the volumes/pans/sends written in the smoke ARE there) — Live just hides the UI surfaces until the chain is populated. Add to push skill prose: warn users that empty-chain tracks have invisible mixer until devices land in phase 7. Not a Hallucinote bug; a Live 12.4 UX quirk worth surfacing. (real-Live smoke, feat/wave-4 W4-E, 2026-05-18)
- **`sidechain_trigger`: principled section-clamp for the first attack window.** W4-B exposed that `sidechain_trigger`'s first `attack_start = hit - attack_beats` can fall before the section boundary, so the envelope's beat range spills outside the chorus's session-clip placement and the planner skips it. falling-walking's build.py works around this by shifting EVERY chorus kick by `attack_beats` (~5ms late at 132bpm). Principled fix: add an `envelope_start_beats` parameter to `sidechain_trigger` (or have it floor `attack_start` to a configurable section start), so the generator clips the pre-attack window without shifting the hits themselves. Sized as: ~10 LoC in `generators/envelopes.py` + 2 tests + revert falling-walking's build-site shift. (critic feat/wave-4 W4-B W1, 2026-05-18)
- **W4-B planner: multiple distinct covering clips on the same track.** `_resolve_envelope_session_clip` breaks on first match (push.py:1191-1197). If two distinct session clips on the same track ever overlap in the arrangement and both cover an envelope's beat range, the planner makes an opaque "earliest start_bar, then earliest id" choice. Not exercised today (falling-walking has unique placements). Defensive fix: emit a `plan.warn` when more than one distinct session clip covers the range, naming both. Sized as: ~5 LoC + 1 test. (critic feat/wave-4 W4-B N1, 2026-05-18)
- **W4-B planner: trimmed placements where end_bar - start_bar < source.length_beats.** Coverage uses the SOURCE session clip's `length_beats` rather than the placement's `end_bar`. A placement that trims the clip shorter than its natural length will still appear to "cover" envelopes past the trim point, but those late breakpoints are inaudible during arrangement playback (the arrangement clip's effective range stops at end_bar). Defensive fix: emit a `plan.warn` when env_max exceeds the trimmed extent (placement_end_beats < source.length-derived end). Sized as: ~3 LoC + 1 test. (critic feat/wave-4 W4-B N2, 2026-05-18)
- **W4-B planner: rule-of-three consolidation across the three `_emit_*_envelope` helpers.** `_emit_mixer_envelope` / `_emit_send_envelope` / `_emit_device_parameter_envelope` all follow the same shape: compute envelope range → resolve placement → check clip link → translate breakpoints → emit + extra-placement warn. Differences are local (target_kind, parent_arg, args dict, error text). Worth consolidating into a single `_resolve_and_translate_to_session_clip` helper that returns a `(clip_at, local_bps, placement)` tuple or `None`. Sized as: ~50-line refactor of the three emitters, no behavior change. (critic feat/wave-4 W4-B N3, 2026-05-18)
- **`_FINGERPRINT_PATHS` binary-safety invariant lives only in the docstring.** W4-F's `_hash_file` normalizes `\r\n → \n` before hashing on the assumption that every fingerprinted entry is Python source (true today). A future contributor adding a non-Python entry (a JSON manifest, an `.so`, a static `.als` skeleton) could silently corrupt the hash because the byte replacement isn't safe for arbitrary binaries. Options: (a) skip normalization for entries that fail a NUL-byte sniff, (b) import-time validator that asserts each resolved fingerprint file ends in `.py`. Tiny — one helper + one test. (critic feat/wave-4 W4-F N1, 2026-05-18)
- **duplicate_to_arrangement: spurious-clip detection is start-time-only.** Chunk W2-H detects the B-24 side effect by comparing arrangement_clips' start_times before vs after the call. Edge case: if a pre-existing clip already sits exactly at `dest_beats + source.length`, its start_time is already in the before-set, and the new spurious clip slips past detection (because both have the same start_time key). A more robust scheme would use object identity (`id(c)`) for the diff — though Live's wrapper recreation (B-1) makes that fragile too. Unlikely in real songs; backlog candidate. (critic W2 N2, Chunk W2-H 2026-05-18)
- **Confirm Phaser/Flanger and Eq3/FilterEQ3 round-trip class_name preservation in real Live.** Chunk W2-B's `device_names` table maps both `Phaser` and `Flanger` to display `Phaser-Flanger` (similarly `Eq3` / `FilterEQ3` → `EQ Three`, `AutoPan` → `Auto Pan-Tremolo`). Live merged these device families under one browser node in 12.x. Load works for either source class_name, but when the device is then captured (`device.list`), the reported `class_name` may be the merged form rather than the original — breaking deterministic re-push for songs that captured a `Phaser` (which would re-capture as `Flanger` after push or vice versa). During the Wave-2 real-Live validation pass, load via `kind='Flanger'`, then re-capture and verify `class_name`. If it isn't preserved, the planner needs to emit `preset_uri` for these merged-display devices instead of relying on class-name resolution. Sized as: real-Live verification + (if needed) ~50 LoC planner change. (critic W2 N1, Chunk W2-B 2026-05-18)
- **Enrich the flat-wire JSONSchema with `enum` / `minimum` / `maximum` / `description` from `ParamSpec`.** Chunk W2-1 synthesized per-tool wrappers whose signature exposes every action's params as keyword-only Optionals — FastMCP's JSONSchema then surfaces them at the top level (matches every doc/example). But the schema today only propagates Python type + Optional; `ParamSpec` already carries `enum` (e.g., `target_kind` across the 7 envelope families), `minimum` / `maximum` (e.g., `cc_number` 0-127, `bpm` 20-999), and `description`. The dispatcher still enforces these at runtime via teaching errors so correctness is intact, but agents could prune impossible calls earlier with a richer schema. Wire it via `typing.Annotated[type, Field(...)]` or by manually editing the generated schema post-registration. Sized as a small refactor in `hallucinote_mcp/src/hallucinote_mcp/server.py::_register_tool` + a wire-schema test confirming enum/min/max round-trip. (critic W2, Chunk W2-1 2026-05-18)
- **FastMCP private-API access in `test_server.py`.** Three new tests in Chunk W2-1 (`test_tool_call_via_fastmcp_accepts_flat_kwargs`, `test_tool_call_via_fastmcp_drops_unsupplied_optional_kwargs`, `test_help_action_works_via_fastmcp_with_no_kwargs`) reach into `mcp._tool_manager._tools[name]` directly to fetch a `Tool` for `.run()`. The existing `registered_tool_names` helper tries multiple attribute names for FastMCP version-drift resilience; a symmetric `get_registered_tool(mcp, name)` helper would centralize the version-coupling and keep these tests robust. (critic W2 N1, Chunk W2-1 2026-05-18)
- **Return-track coverage for clip-scoped automation paths.** Chunk D added clip-scoped `mixer_volume` / `mixer_pan` / `send_level` / `device_parameter` paths via `_require_parent`'s `return_index` branch but the new unit tests only exercise the `track_index` branch. Live 12.4 treats returns as tracks with clip slots + arrangement clips, so the code path is structurally the same; a parity test guards against regressions in `_require_parent` or the return-side clip resolution. Sized as 3-4 tests in `test_actions_automation.py`. (critic, Chunk D 2026-05-18)
- **Clear + note_expression: omit-required-args path untested.** Today, `ableton_automation(action='clear', target_kind='note_expression')` raises the gap-citing `NotImplementedError` regardless of whether note_pitch / note_start_beats / axis were supplied (the gap check fires before parameter validation). Asymmetric with `write_envelope` which validates first. Either add a docstring note acknowledging the precedence or add a one-line test pinning the behavior. (critic, Chunk D 2026-05-18)
- **Backlog: return-side device_parameter envelopes need a return-track session-clip model.** W4-B routes track-side mixer/pan/send/device_parameter envelopes through session clips on the parent track. Return tracks have arrangement-side mixer state but the DB has no session-clip model for returns (`clips.track_id` references `tracks(id)` only). Live 12.4 only accepts device_parameter envelopes on session clips, so return-side envelopes get a warn+skip today. Closing the gap is non-trivial: add a return-clip schema branch + mutators + push/pull routing. Not blocking V1 — falling-walking has no return-side envelopes. (W4-B 2026-05-18)
- **W4-C suffix-only invariant lacks structural enforcement.** W4-C established that `returns.name` stores SUFFIX-only (Live re-adds the slot prefix on push). The invariant is enforced only by writer discipline (`capture.py` strip + `pull.py` strip); a future song `build.py` calling `M.create_return(conn, name="A-Reverb", ...)` would silently re-poison the DB. Options: (a) add `CHECK (name NOT GLOB '[A-Z]-*')` to the `returns` schema, (b) push the strip into `M.create_return` / `M.update_return` (the only sanctioned write paths) so all callers benefit automatically. Option (b) better fits the project's mutator discipline. Sized as: small mutator change + schema CHECK + 1-2 tests. (critic feat/wave-4 W4-C N1, 2026-05-18)
- **W4-C send warnings use mixed identity convention.** `_apply_track_sends_for_track` warns with `raw_name` (Live's prefixed form) for the "no matching return" case but with `return_name` (stripped DB form) for the "rejected by mutator" case. Both are defensible — the first identifies what Live reported (helpful for "where is this come from"), the second identifies the DB row (consistent with the detail messages) — but a debugger seeing both warnings for the same send sees two different identifiers for the same return. Pick one convention and document it (or surface BOTH forms in every warning: `"Live='A-Reverb' DB='Reverb'"`). Sized as: ~5 LoC + doc comment. (critic feat/wave-4 W4-C N2, 2026-05-18)
- **Wave plan headers missing top-level `Requirements Confidence` field.** Each M+1 chunk has an inline Confidence check, but the wave-level header in `build-plan.md` lacks a `Requirements Confidence: High|Medium|Low` declaration. Methodology cleanup — apply to the next wave header (M+2 or whatever follows) rather than retrofitting M+1's plan. (critic, M+1 final 2026-05-17)
- **Arrangement-VIEW state pull (loop region, follow mode, view zoom).** Distinct from arrangement-clip-placement pull (which M+1-3b ships). `ableton_arrangement(action='info')` exposes the view state but there is no DB home for loop region or view zoom today, and the tempo/signature it returns are better diffed against `tempo_map`/`time_signature_map` via dedicated probes that match the schema's map shape. Decide whether to add DB columns for view state (probably on a `ableton_sessions` row, as it's session-bound view state, not authored song data) — or leave view state non-round-tripped per Vision's "DB is the score, not the rehearsal-room state." Filed for explicit decision, not silent drop. (reflection, M+1-3 re-plan 2026-05-17)
- **MCP gap: `hallucinote-mcp` envelope read surface.** Wave M-4 unified envelope WRITES via `ableton_automation(action='write_envelope', target_kind=…)` across all seven target families (`clip_cc`, `clip_pitch_bend`, `note_expression`, `device_parameter`, `mixer_volume`, `mixer_pan`, `send_level`). No symmetric READ exists — no `list_envelopes`, no `get_envelope`. Live exposes envelopes; this is purely a `hallucinote-mcp` server gap (not a Live API limitation). Required for envelope pull. Per-target-family read actions probably (mirror the write `target_kind` enum). (V1 close-out 2026-05-17; promoted from inline `docs/mcp-requirements.md` "Capture-side read" §)
- **MCP gap: nested rack chain probe + push.** Live supports rack devices (`DrumGroupDevice`, `InstrumentGroupDevice`, `AudioEffectGroupDevice`) that own nested device chains. Hallucinote's schema *models* nested chains (via `device_chains.parent_rack_device_id`) but capture/replay/push stay flat — `hallucinote-mcp` exposes no probe inside racks. Required: `get_device_chains(track_index, device_index)` → per-chain device list; `load_device_in_rack(track_index, parent_device_index, chain_index, position, kind, preset_uri?)`; `set_rack_device_parameter(track_index, parent_device_index, chain_index, device_index, parameter_name, value)`. Sizable ask but drum kits + modular-style instruments don't fully round-trip without it. Affects: capture, pull, push. (V1 close-out 2026-05-17; merges + sharpens the prior "Capture extension for nested rack chains" entry which was incorrectly framed against gap #17b)
- **Live API residual on gap #4: true surgical Ableton-side note writes.** V1 close-out Chunk D shipped READ-with-stable-IDs + WRITE-whole-clip (sufficient for compose/produce: `ableton_note(action='list')` + `clip-notes` pull domain + `ableton_clip(action='replace_notes')`). The residual gap is per-note Ableton writes that preserve playback continuity (Live retriggers a clip on `set_notes` during playback) — would land via `apply_note_modifications` / `add_new_notes` / `remove_notes_by_id`. Out of V1 scope per `scope.never` (no live-performance use case). Re-open if/when performance use cases enter scope. (V1 close-out 2026-05-17; updated after Chunk D shipped)

- **Hallucinote-side quantize / swing / groove module.** During Wave M-3, the user reaffirmed the DB-as-source-of-truth principle for note timing: quantize, swing, and groove templates are pure-math transforms that should live in Hallucinote (Python/SQL), not call Live's `clip.quantize()` or use Live's Groove Pool. The MCP surface therefore exposes NO `quantize` / `apply_groove` / `extract_groove` actions; Hallucinote computes the result and pushes pre-grooved notes via `ableton_clip(action='replace_notes', ...)`. Scope: (a) `src/hallucinote/timing/quantize.py` — pure functions taking `(notes, grid, amount, swing) → notes`; (b) `grooves` table for storable timing templates portable across songs; (c) `apply_groove(notes, groove_template) → notes` and `extract_groove(notes) → groove_template`; (d) generator-layer wiring so `generate_*` outputs can optionally pass through quantize/groove before persistence. Sized as a single chunk after Wave M completes (or sooner if a song needs it). Test discipline: example-based fixtures for known input→output; consider Hypothesis property tests for the round-trip invariant (extract-then-apply approximately recovers the template). (reflection, Wave M-3 user pivot 2026-05-17)
- **Audio render + analysis (`ableton_render` + `ableton_analysis`).** User-flagged as "sooner than later, but not yet." Sketched in `docs/mcp-tool-design.md` §17 (Tier 3). Async-shaped (render returns job handle; analysis takes file path). Wave M+1 candidate after `hallucinote-mcp` v1.0 ships. Open question: analysis lives in `hallucinote-mcp` or a separate `audio-analysis-mcp`? (reflection, session 2026-05-16)
- **Wider legacy-tool prose rewrite in `songs/falling-walking/falling-walking.md`.** J-3 caught the two explicit `mcp__AbletonMCP__*` references (line 226 sibling-repo path + line 280 `list_external_plugins`) but the "Tool quirks to know" block at lines 281-284 (`get_cue_points`, `add_notes_to_clip`, `create_clip`, `duplicate_clip_to_arrangement`) and line 227 ("AbletonMCP control surface is installed at the Music path") still use the legacy fork's mental model. These need an agent-consumption-focused rewrite against the v1.0 `mcp__hallucinote-mcp__*` action-dispatch surface. Sized as a small docs chunk; defer until the song agent prose actively misleads someone. (janitor, J-3)
- **Module-size watch: `src/hallucinote/db/mutations.py` (2117 lines), `sync/push.py` (1333 lines), `sync/pull.py` (1089 lines).** Cohesion is high (per-domain helpers cluster naturally), but at ~4.5kLOC across three files the per-domain split (e.g. `mutations/clips.py`, `mutations/devices.py`, `mutations/automation.py`) becomes attractive once the suite exceeds ~30s or a new contributor onboards. No action today; revisit when either trigger fires. (janitor, J-3)
- **Name collision: `tools/capture.py` (CLI wrapper, ~50 lines) vs `src/hallucinote/capture.py` (412-line module).** Both files share a leaf name; the CLI wrapper just invokes `python -m hallucinote.capture` style. A fresh reader can be momentarily confused about which is "the capture module." Either rename the CLI wrapper (e.g. `tools/capture-cli.py`) or add a one-line header comment to both pointing at the other. Non-blocking. (janitor, J-3)
- **Migration tool `tools/migrate_arrangement_clip.py` has no automated tests.** Manual round-trip verified end-to-end on falling-walking's real DB (32 placements + 32 events + 3 indexes rewritten; second run no-ops). A small synthetic-fixture test would lock the contract — particularly the JSON1 payload-key rewrite and the idempotency guard against both-tables-present. Sized as a single test file under `tests/unit/tools/`. (PR review #19 + #24, 2026-05-17)
- **Two raw `conn.execute("SELECT ...")` reads in `sync/push.py:996, 1053`.** Not a discipline violation (reads, not writes) but factoring into `queries.py` would localize SQL exposure consistently with the rest of the project. (PR review #24, 2026-05-17)
- **Note pull: no full push-mutate-pull round-trip test.** The `test_pull.py` round-trip section exercises mix-state and clip-level diffs but not the new `clip-notes` domain end-to-end. Individual diff classes are well-tested in isolation; the round-trip would catch any planner/apply integration drift. Sized as one test in the existing round-trip section. (PR review #23, 2026-05-17)
- **`_apply_session_clips_for_track` cascades `delete_clip` → `arrangement_clips` silently.** Design-consistent with the project's cascade discipline, but the cross-domain side effect is invisible in `out.details` / no per-row events for the cascaded placements. Worth a follow-up to count + log cascaded arrangement-clip placements when a session-clip delete fires during pull. (PR review #22, 2026-05-17)
- **`_apply_session_clips_for_track` silently tolerates missing `length` / `name` on populated entries** (via `_floats_differ(None, X) → False`), unlike `_apply_arrangement_clips_for_track` which warns explicitly on missing fields. Asymmetry, not a correctness bug. Tighten for parity. (PR review #22, 2026-05-17)
- **`docs/mcp-requirements.md` heading prose under gap #4 still uses pre-Chunk-D framing.** The status summary table and the line-54 backlog rollup were both flipped to "partial"; the deeper Priority-1 narrative still describes note-level addressing as fully blocked. Update the prose to match the partial-shipped state. (PR review #23, 2026-05-17)
- Capture extension for nested rack chains. The chunk-4 snapshot has racks (`InstrumentGroupDevice`, `DrumGroupDevice`) flagged with `_note: "Rack — internal chain instruments not captured"`. Gated on the broader "MCP gap: nested rack chain probe + push" entry above — once `get_device_chains` lands, extend `tools/capture.py` + `compile_snapshot` to recurse into rack chains and populate `device_chains` with `parent_rack_device_id`. (builder, chunk-4 setup; updated 2026-05-17 — the prior "gap #17b" framing was incorrect)
- Master-strip device chains. `device_chains.parent_track_id` accepts the `kind='master'` row, but `plan_push_devices` skips `kind='master'` today. Master devices (master limiter, master EQ) are common in real mixes — needs a master-specific push path (no track_index, reaches the master strip directly) and an MCP gap entry. falling-walking has no master devices today, so this is non-blocking. (builder, chunk 4a; updated 2026-05-17 after the `'return'` kind reservation was dropped)
- Audio clips: clip kind discriminator, file references, warp metadata, warp markers. Deferred from V1. (migrated)
- **Session-view audio clip placement via browser-load workaround (W6-D 2026-05-19 investigation).** Live 10–12 has no `ClipSlot.create_audio_clip` or equivalent — confirmed via LOM XML (Structure-Void Live 11.0), M4L LomTypes whitelist, Push remote scripts, and Live 12 release notes. The only path to put audio in a session slot is async browser-load: set `song.view.highlighted_clip_slot = target_slot`, then `application.browser.load_item(audio_browser_item)`. Caveats: async (no completion callback), audio must be addressable as a BrowserItem (Library/User/Places — not an arbitrary filesystem path), browser-indexing dependent. Could expose as `ableton_clip(action='load_audio_to_session', track_index, clip_index, browser_uri)` mirroring `ableton_device(action='load')`'s browser-driven pattern (W6-D investigation; backlog 2026-05-19)
- Track routing: sidechain, parallel busses, input/output routing config. Schema + sync work. (migrated)
- Group tracks (`tracks.parent_track_id` + Live group semantics). (migrated)
- Pull-side sync follow-on (status as of W5-D 2026-05-19): **Shipped:** mix-state (W3-1), cue points + score globals (W3-2), device chain structure (W3-3 / M+1-2), arrangement-clip placements (W3-4 / M+1-3b), session-view clip slots (V1 close-out Chunk C), note pull via stable-ID read + whole-clip write (V1 close-out Chunk D — gap #4 partial), device-parameter values (W5-D). **Deferred (backlog entries above):** envelope pull (gated on `hallucinote-mcp` envelope read surface), nested rack chain pull (gated on `hallucinote-mcp` `get_device_chains`). (migrated; rewritten V1 close-out 2026-05-17; W5-D update 2026-05-19)
- Event replay function (`replay(events) → state`) and merge tooling. Required for cross-DB event-stream merges between collaborators; only useful after the event-store flip. (migrated)
- Post-hoc event annotation (`event_annotations` table) for narrative on-the-fly addition to history. (migrated)
- Real-time concurrent editing / cloud DB migration. Out of foreseeable scope; the mutator-discipline + storage abstraction already keeps the door open. (migrated)
- Song tests (on-demand layer): DB consistency + mix hygiene + audio/spectral. Land per-chunk as appetite allows; not gating any chunk's completion. (migrated)

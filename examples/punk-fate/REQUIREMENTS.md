# Requirements — Punk Fate

Song slug: `punk-fate`

This file lists the third-party plugins this song uses. Install them in Ableton Live before pushing the song. Generated by `"<python>" -m hallucinote.cli compat write-requirements <slug>` — re-run after material changes to the song's device list.

## Required third-party plugins

None. This song uses only Live's built-in devices — no additional installs needed.

## preset_query authoring issues

These devices have structurally-valid ``preset_query`` selectors that were never resolved against a browser — no dry-run map was supplied, so this report can't say whether each one matches exactly one device. Verify against a running Live (the push preflight resolves them) before trusting the load:

- **Hard Punch** at 01 Drums position 2 — _preset_query_unverified_
  - no browser dry-runs provided (run `compat check <slug> --probe` with Live + the MCP bridge available)
- **Drum - Rock Parallel Room** at 01 Drums position 3 — _preset_query_unverified_
  - no browser dry-runs provided (run `compat check <slug> --probe` with Live + the MCP bridge available)
- **Gritty Bass** at 02 Bass position 2 — _preset_query_unverified_
  - no browser dry-runs provided (run `compat check <slug> --probe` with Live + the MCP bridge available)
- **Bass - Punch** at 02 Bass position 3 — _preset_query_unverified_
  - no browser dry-runs provided (run `compat check <slug> --probe` with Live + the MCP bridge available)
- **Guitar - Controlled Dynamics** at 03 Guitar position 2 — _preset_query_unverified_
  - no browser dry-runs provided (run `compat check <slug> --probe` with Live + the MCP bridge available)
- **Guitar Dirt** at 03 Guitar position 3 — _preset_query_unverified_
  - no browser dry-runs provided (run `compat check <slug> --probe` with Live + the MCP bridge available)
- **Guitar - Lead Solo** at 04 Voice position 2 — _preset_query_unverified_
  - no browser dry-runs provided (run `compat check <slug> --probe` with Live + the MCP bridge available)
- **Rough Tone** at 04 Voice position 3 — _preset_query_unverified_
  - no browser dry-runs provided (run `compat check <slug> --probe` with Live + the MCP bridge available)

## Live built-in devices (no install needed)

Listed here for completeness — these ship with Live and don't need separate installation:

`Amp`, `Audio Effect Rack`, `Auto Pan Legacy`, `Cabinet`, `Chorus`, `Delay`, `Drum Rack`, `EQ Eight`, `Instrument Rack`, `Limiter`, `Note Length`, `Pitch`, `Reverb`, `Sampler`, `Simpler`, `Utility`.

---

_The song's DB is the source of truth for device counts and positions; this file is a human-readable summary._
